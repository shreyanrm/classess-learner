/**
 * @classess/vidya — the signature tutor (docs/02-DESIGN/02-vidya.md).
 *
 * Her identity is LOCKED and encoded in `identity.ts` (asserted by tests). Her choreography is free:
 * pages compose her mood, gaze, and flame per surface (the Vidya-cute license). Freedom in
 * choreography, never in identity.
 */
export * from './identity';
export * from './VidyaPanel';
export * from './VidyaPresence';
