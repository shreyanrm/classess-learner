/**
 * Every board on earth, on demand (docs/CURRICULUM.md) — the client half.
 *
 * The app imports this and nothing else for anything a learner studies. There is no bundled
 * catalog behind it: the registry lives in the brain, and what is not in the registry yet is a
 * discovery job with an honest status, or the learner's own syllabus.
 */

export * from './client';
export * from './labels';
export * from './overlay';
export * from './parse';
export * from './types';
