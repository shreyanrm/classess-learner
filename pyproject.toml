[project]
name = "classess-learner-workspace"
version = "0.0.0"
description = "Wobo — Python services workspace (BFF, gateway, verifier, Plexus)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.2", "ruff>=0.6"]

[tool.uv.workspace]
# Members are added as each Python package is created. Globbing avoids editing this on every add.
# content/catalogs is data (board catalog JSON), content/cache is the Plexus artifact
# cache (written at runtime by the gateway), and content/factbase is the NCERT fact-base data +
# standalone build scripts — none is a Python package.
# services/render-worker has no pyproject.toml (a Node/TS render service with a Python queue
# consumer), so the Python workspace glob must skip it — otherwise every `uv run` fails trying to
# resolve it as a member. Drop this exclude if it later becomes a real uv package (adds pyproject).
members = ["services/*", "content/*"]
exclude = ["content/catalogs", "content/cache", "content/factbase", "services/render-worker"]

[tool.ruff]
line-length = 100
target-version = "py312"
# services/render-worker is a Node/TS render service (see [tool.uv.workspace] above); its Python
# files (the queue seam + the operator-run drain worker) are standalone scripts, not part of the
# linted gateway codebase — excluded here for the same reason it is excluded from the uv workspace.
extend-exclude = ["docs", ".venv", "services/render-worker"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "C4"]

[tool.ruff.lint.per-file-ignores]
# Prompt- and data-heavy modules: an LLM system prompt, a calibration dataset, and spike prose
# have legitimately long lines. E501 is noise there; all real code-quality lints stay enforced.
"content/atom/**" = ["E501"]
"services/gateway/src/classess_gateway/wobo.py" = ["E501"]
# engine prompts + inline SVG/JSON string literals: legitimately long lines, E501 is noise
"services/gateway/src/classess_gateway/plexus/engines.py" = ["E501"]
# subject validators: doctrine docstrings + inline self-check data (same justification as engines.py)
"services/gateway/src/classess_gateway/plexus/bio.py" = ["E501"]
"services/gateway/src/classess_gateway/plexus/social.py" = ["E501"]
"services/gateway/src/classess_gateway/plexus/maps.py" = ["E501"]
# email templates are inline-styled HTML string literals — long lines are the format, not slop
"services/gateway/src/classess_gateway/email_templates.py" = ["E501"]
"services/gateway/tests/test_wobo.py" = ["E501"]
# the fact-base builder and its patch script: LLM system prompts, run-command docstrings, and
# embedded Python source strings. Same justification as content/atom/** — every other lint stays on.
"content/factbase/**" = ["E501"]
# GENERATED from packages/wobo/src/board/schema.ts (bun run --cwd packages/wobo
# board:codegen). The grammar travels as one JSON literal per schema so the mirror is
# byte-stable; formatting it by hand would be edited away on the next regeneration.
"services/gateway/src/classess_gateway/board_schema.py" = ["E501", "B905"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["services", "content"]
