export const meta = {
  name: 'wobo-rebrand',
  description: 'Rebrand Classess Learner + the tutor Vidya to Wobo across the whole monorepo, then gate and prove it',
  phases: [
    { title: 'Rename', detail: 'one Opus agent: scripted global rename + file moves + install' },
    { title: 'Refine', detail: 'parallel Opus agents: docs, gateway, web copy + assets, contracts, tests' },
    { title: 'Gate', detail: 'Opus: full typecheck/lint/test/build/pytest + leftover sweep, fix' },
    { title: 'Proof', detail: 'Sonnet: dev server screenshots of the rebranded surfaces' },
  ],
}

const REPO = args.repo
const SHOTS = args.shotsDir
const INTRO = `OWNER-DIRECTED FIRST-TIME INTRODUCTION (2026-09-02): the very first time a learner meets Wobo (onboarding, before any other line), she introduces herself with EXACTLY this copy, shown letter by letter in her handwriting and spoken by TTS: "Hey there. I'm Wobo, your AI wobot. I'll help you learn, and I'll be with you every step of the way." Rules: "wobot" (Wobo + robot) is her word for what she is — add it to her persona so chat and voice use it naturally ("your AI wobot"); the opener "Hey there." is deliberately a two-word sentence so voice starts fast; returning learners are greeted by name and NEVER re-introduced; keep the register (sentence case, no emoji, no exclamation marks).`
const COMMON = `Repo: ${REPO} (Bun workspaces monorepo; branch the-life). You are part of a coordinated rebrand. The product "Classess Learner" is now "Wobo", AND the tutor character "Vidya" is now also "Wobo" (one name: Wobo is the app and Wobo is the tutor who lives in it; she keeps her existing pronouns, body, voice and behaviour — only the name changes). "Classess" survives ONLY as: the company/ecosystem name in the law docs (Classess Teacher, Classess School, the Dot eVentures ecosystem), and internal identifiers that must not change: package scope @classess/*, python package classess_gateway, service/Railway names classess-learner, hostnames learner.classess.com / mail.classess.com / *.vercel.app origin regexes, the X-Classess-Internal header, logger names, CSS custom-property prefix --clss-. Do NOT touch: bun.lock/uv.lock by hand (bun install regenerates), binary files, xbrowser/, respdiag/, shots/, .playwright-mcp/, node_modules. Never run git commit/push. Work only inside the repo. When done, return ONLY the structured output.`

const RESULT = {
  type: 'object', required: ['summary', 'changed_files', 'notes'],
  properties: {
    summary: { type: 'string' },
    changed_files: { type: 'array', items: { type: 'string' } },
    moved: { type: 'array', items: { type: 'string' }, description: 'old -> new' },
    counts: { type: 'object', additionalProperties: { type: 'integer' } },
    gates: { type: 'object', additionalProperties: { type: 'string' }, description: 'command -> pass/fail + counts' },
    leftovers: { type: 'array', items: { type: 'string' }, description: 'file:line snippets still containing Vidya/Classess Learner that you deliberately left, with reason' },
    notes: { type: 'string' },
  },
}

phase('Rename')
const rename = await agent(`${COMMON}
YOU are the RENAME agent: do the scripted global rename first so the other agents work on renamed code.
1. Precondition: cd ${REPO} && git status --porcelain must show only untracked files plus the already-staged brand assets (apps/web-pwa/public/*.png, apps/web-pwa/public/favicon.svg, apps/web-pwa/src/ui/Logo.tsx, services/render-worker/public/wobo-logo*.png). If tracked files other than Logo.tsx are modified, STOP and report.
2. File moves with git mv (update every import/path that references them afterwards):
   - VIDYA.md -> WOBO.md ; VIDYA-CAPABILITIES.md -> WOBO-CAPABILITIES.md
   - apps/web-pwa/src/vidya -> apps/web-pwa/src/wobo
   - packages/vidya -> packages/wobo (package name in its package.json: @classess/vidya -> @classess/wobo; update every workspace dependency and tsconfig path that names it)
   - packages/wobo/src/VidyaPanel.tsx -> WoboPanel.tsx ; VidyaPresence.tsx -> WoboPresence.tsx ; body/VidyaBody.tsx -> body/WoboBody.tsx
   - services/gateway/src/classess_gateway/vidya.py -> wobo.py ; services/gateway/tests/test_vidya.py -> test_wobo.py
   - apps/web-pwa/tests/vidya-capabilities.spec.ts -> wobo-capabilities.spec.ts
   - apps/web-pwa/wave4-vidya-full-capacity.workflow.js -> wave4-wobo-full-capacity.workflow.js
   - docs/02-DESIGN/02-vidya.md -> 02-wobo.md ; docs/03-FLOWS/04-vidya-flow.md -> 04-wobo-flow.md
   - apps/web-pwa/public/classess-logo.png and services/render-worker/public/classess-logo.png: git rm (replacements already staged as wobo-logo.png).
3. Text replacement over TRACKED text files only (get the list with: git ls-files | grep -vE '\\.(png|jpe?g|gif|mp4|mov|wav|mp3|pdf|woff2?|ico)$' | grep -vE '^(xbrowser|respdiag|shots|\\.playwright-mcp)/' | grep -vE '(bun|uv)\\.lock$'), using perl -pi with these ordered rules:
   a. "Classess Learner" -> "Wobo"
   b. "Vidya's" -> "Wobo's" ; "Vidya" -> "Wobo" ; "vidya" -> "wobo" ; "VIDYA" -> "WOBO" (this also renames identifiers: VidyaBody->WoboBody, useVidyaBus->useWoboBus, VidyaMood->WoboMood, vidya.turn->wobo.turn, classess/vidya-tutor-slm->classess/wobo-tutor-slm, CSS classes, test names, cached content text under content/).
   c. In these user-facing places ONLY, standalone "Classess" -> "Wobo": apps/web-pwa/index.html, apps/web-pwa/vite.config.ts (manifest name AND short_name -> "Wobo"; description -> "Learn with Wobo — mastery-first courses, practice, and a companion who knows you."), services/gateway/src/classess_gateway/email_templates.py (title, wordmark span, footer "Wobo · made for curious minds", subject "welcome to Wobo"), services/gateway/src/classess_gateway/email.py (_FROM = "Wobo <wobo@mail.classess.com>"), services/gateway/src/classess_gateway/app.py (FastAPI title "Wobo model gateway"), services/verifier/src/classess_verifier/app.py (title "Wobo correctness verifier"), services/render-worker/src/Explainer.tsx (watermark comment + staticFile('wobo-logo.png')), apps/web-pwa/src/engines/MotionPlayer.tsx comments, apps/web-pwa/src/ui/cast/index.tsx comment, README.md title/intro. Do NOT blanket-replace "Classess" anywhere else.
   d. Logo: every import/use of ClassessLogo -> WoboLogo, then DELETE the temporary "export const ClassessLogo = WoboLogo" alias and its ponytail comment at the bottom of apps/web-pwa/src/ui/Logo.tsx (Logo.tsx already exports WoboLogo; call sites: ui/AppHeader.tsx, engines/MotionPlayer.tsx, screens/concepts/ConceptA/B/C.tsx). In index.html: <title>Wobo</title>, replace the inline data-URI favicon link with <link rel="icon" type="image/svg+xml" href="/favicon.svg" />, add <meta name="description" content="Learn with Wobo — mastery-first courses, practice, and a companion who knows you." />.
   e. Gateway compatibility alias: wherever capability names are resolved for POST /v1/capability/{name} (registry.py / routing.py / app.py), map the legacy name "vidya.turn" to "wobo.turn" with a one-line alias and a comment (the currently deployed web bundle still calls vidya.turn until the web redeploys). Add a test for the alias in the gateway tests.
4. cd ${REPO} && bun install (regenerates bun.lock for the package rename). Then: bun run typecheck ; bunx biome check --write . ; bun run test ; cd services/gateway && uv run pytest -q ; cd ${REPO} && uvx ruff check services content. Fix every failure you caused. Record pass/fail per gate.
5. Sweep: git grep -n -i -E 'vidya|classess learner' -- . ':!bun.lock' ':!uv.lock' ':!xbrowser' ':!respdiag' ':!shots' ':!*.png' ':!*.jpg' ':!*.mp4' — report every remaining hit in leftovers with the reason you left it (there should be none except deliberate historical mentions, which you should not create).
Return counts per rule, moved list, gates, leftovers.`, { label: 'rename:global', phase: 'Rename', schema: RESULT, model: 'opus', effort: 'high' })
if (!rename) throw new Error('rename agent died')
log(`Rename: ${rename.summary}`)

phase('Refine')
const REFINERS = [
  { key: 'docs', prompt: `YOU are the DOCS refiner. The mechanical rename already turned "Classess Learner"->"Wobo" and "Vidya"->"Wobo" in: README.md, CONTEXT.md, DESIGN.md, WOBO.md, WOBO-CAPABILITIES.md, MOTION.md, DECISIONS.md, DEPLOY.md, HANDOFF.md, SUBJECTS.md, VIDEO-QUALITY.md, CONTENT-VISUALS.md, PHASE-0-REPORT.md, PHASE-1-REPORT.md, docs/**. Read each of the root docs fully and docs/** at least by skimming every file, and fix prose that the mechanical rename made wrong or ambiguous: (1) where the app and the tutor appear in one sentence, disambiguate as "the Wobo app" vs "Wobo (the tutor)" or rephrase; a sentence like "Wobo is the life of Wobo" must be rewritten; (2) the ecosystem/company "Classess" (Classess Teacher, Classess School, Dot eVentures ecosystem) stays "Classess" — make sure no such reference was accidentally changed and that the docs now say Wobo is the flagship consumer product of the Classess ecosystem; (3) headings/anchors/cross-links that pointed at VIDYA.md or VIDYA-CAPABILITIES.md now point at WOBO.md / WOBO-CAPABILITIES.md; (4) DECISIONS.md: add an entry dated 2026-09-02 "Rebrand: Classess Learner -> Wobo; the tutor Vidya -> Wobo (one name for app and tutor); identifiers, package @classess/wobo, capability wobo.turn (legacy vidya.turn aliased on the gateway for one release), law docs renamed; the Classess name is retained for the ecosystem and its B2B products; contract event types containing the old name were renamed pre-launch (no production event lake existed; the Supabase project was paused at the time)"; (5) DESIGN.md/DECISIONS.md lines like "molten stays Vidya's alone" must now read naturally. Keep the docs' register (sentence case, no emoji, no exclamation marks, calm and certain).` },
  { key: 'gateway', prompt: `${INTRO}\nYOU are the GATEWAY refiner. In wobo.py's persona/system prompt add: she is "Wobo, an AI wobot"; she may call herself a wobot; she introduces herself with the owner's exact line ONLY when the request context marks a first meeting (find how the web client signals first-run/onboarding to the gateway, or add a small optional flag such as payload.first_meeting=true handled in the prompt builder), and otherwise greets by name without re-introducing. Read services/gateway/src/classess_gateway/wobo.py, app.py, routing.py, registry.py, voice.py, safety.py, email.py, email_templates.py and services/verifier + services/render-worker sources. Fix anything the mechanical rename made wrong in prompts and copy: persona prompts must introduce the tutor as Wobo naturally ("You are Wobo, ..."), no doubled "Wobo Wobo", no sentence where the app and the tutor collide confusingly; email copy signs "— Wobo" and the footer/sender read well; TTS/voice greeting strings; any docstrings. Confirm the vidya.turn -> wobo.turn alias exists and is tested; confirm registry model ids and capability names are consistent between registry.py, the web client (apps/web-pwa/src/wobo/**), and services/gateway/tests. Run cd services/gateway && uv run pytest -q and uvx ruff check services; fix what you broke.` },
  { key: 'web', prompt: `${INTRO}\nYOU are the WEB COPY + ASSETS refiner. FIRST: find the onboarding greeting beat (apps/web-pwa/src/screens/Onboarding.tsx and its speech/handwriting helpers in src/wobo/**) and make the first line the learner sees and hears the owner's exact intro, written letter by letter in her handwriting and spoken through the existing TTS path (keep the two-word first sentence intact so audio starts fast); make sure a returning learner (existing profile/session) gets a by-name greeting instead and never sees the intro again; if the web client can pass a first-meeting flag to the gateway's wobo.turn, set it on that first turn. Read apps/web-pwa/src/wobo/**, apps/web-pwa/src/screens/Onboarding.tsx, Home.tsx, screens/home/**, ui/AppHeader.tsx, ui/Logo.tsx, shell/CommandPalette.tsx, engines/MotionPlayer.tsx, index.html, vite.config.ts, and grep apps/web-pwa/src for every user-visible string containing "Wobo" to review it in context. Fix: awkward copy produced by the rename (e.g. "talk to Wobo…" is fine; "Wobo composed this in Wobo" is not), aria-labels, placeholder text, toasts, onboarding lines, the TTS first-sentence rules. Verify the WoboLogo renders in both themes (it uses currentColor; the old --clss-logo-filter CSS variable is now unused — delete any leftover references), the manifest and index.html metadata, the favicon.svg link, the PWA icon files exist (pwa-192.png, pwa-512.png, pwa-maskable-512.png, apple-touch-icon.png, wobo-logo.png in apps/web-pwa/public), and services/render-worker/src/Explainer.tsx uses staticFile('wobo-logo.png'). Run cd ${REPO} && bun run typecheck && bunx biome check --write apps/web-pwa && cd apps/web-pwa && bun run test; fix what you broke.` },
  { key: 'contracts', prompt: `YOU are the CONTRACTS refiner. Read packages/contracts/**, services/contracts/**, packages/sdk/**, packages/config/**. The mechanical rename changed event type strings, schema ids, and generated files that contained "vidya". Determine which files are generated (look for generators/scripts and headers) and regenerate them with the project's own generator if one exists rather than hand-editing; ensure the TypeScript and Python contract packages agree exactly (same event names, same schema ids, same versions); update fixtures/tests; run cd ${REPO} && bun run --filter '@classess/contracts' test, bun run --filter '@classess/sdk' test, bun run --filter '@classess/config' test (or the closest equivalents in their package.json), and cd services/contracts && uv run pytest -q. Fix what you broke. Report every renamed event type / schema id as old -> new.` },
  { key: 'tests', prompt: `YOU are the TESTS refiner. Read apps/web-pwa/tests/**, apps/web-pwa/test/**, apps/web-pwa/src/**/*.test.ts, packages/*/test*, packages/*/src/**/*.test.ts, services/gateway/tests/**, services/verifier tests, services/render-worker/src/plan.test.ts. Update every assertion, fixture, snapshot, selector, aria-name, and title expectation that still expects "Vidya" or "Classess Learner" (the app title is now "Wobo"; the manifest short_name is "Wobo"; email subject "welcome to Wobo"; sender "Wobo <wobo@mail.classess.com>"). Run cd ${REPO} && bun run test and cd services/gateway && uv run pytest -q; fix what you broke. Do not weaken or delete tests to make them pass.` },
]
const refined = await parallel(REFINERS.map(r => () => agent(`${COMMON}\nContext from the rename agent: ${JSON.stringify({ summary: rename.summary, moved: rename.moved, leftovers: rename.leftovers })}\n${r.prompt}`, { label: `refine:${r.key}`, phase: 'Refine', schema: RESULT, model: 'opus', effort: 'medium' })))
const refinedOk = refined.filter(Boolean)
log(`Refine: ${refinedOk.length}/${REFINERS.length} agents returned`)

phase('Gate')
const gate = await agent(`${COMMON}
YOU are the GATE agent. Previous agents: ${JSON.stringify(refinedOk.map(r => ({ summary: r.summary, gates: r.gates, leftovers: r.leftovers })))}.
Run the FULL gates from ${REPO} and fix every failure (root-cause fixes, never skipping or deleting tests): (1) bun install --frozen-lockfile || bun install ; (2) bun run typecheck ; (3) bunx biome check --write . then bunx biome check . ; (4) bun run test ; (5) cd apps/web-pwa && bun run build (record chunk sizes) ; (6) cd services/gateway && uv run pytest -q ; (7) cd services/verifier && uv run pytest -q (if tests exist) ; (8) cd services/render-worker && bun test (if tests exist) ; (9) cd ${REPO} && uvx ruff check services content ; (10) sweep: git grep -n -i -E 'vidya|classess learner' -- . ':!bun.lock' ':!uv.lock' ':!xbrowser' ':!respdiag' ':!shots' ':!*.png' ':!*.jpg' ':!*.mp4' — the ONLY acceptable hits are the DECISIONS.md rebrand entry and the gateway alias line/test that mention the legacy name; fix everything else; (11) git status --porcelain summary (counts of modified/renamed/added/deleted) and confirm no binary/screenshot dirs were touched. Return gates as command -> result.`, { label: 'gate:full', phase: 'Gate', schema: RESULT, model: 'opus', effort: 'high' })

phase('Proof')
const proof = await agent(`${COMMON}
YOU are the PROOF agent. Produce screenshots of the rebranded app for the orchestrator to review. Steps: cd ${REPO}/apps/web-pwa ; read package.json scripts and .env.example to learn how to run the dev server in a no-network/mock mode (look for VITE_LLM_MODE, VITE_DEV_AUTH, VITE_PERSIST_MODE and pick the values that let the app run without Supabase or the gateway, e.g. VITE_LLM_MODE=mock VITE_DEV_AUTH=true VITE_PERSIST_MODE=local — confirm from the code in src/store or src/data what the accepted values are). Start it in the background on a free port (bun run dev -- --port 5177 --strictPort > /tmp/wobo-dev.log 2>&1 &), wait until it answers. Then load the Playwright MCP tools via ToolSearch (browser_navigate, browser_take_screenshot, browser_snapshot, browser_click, browser_resize) and capture PNGs into ${SHOTS}: 01-onboarding-light.png (first screen), 02-home-light.png (after completing onboarding with test values if a dev-auth path exists; otherwise the first screen reached), 03-home-dark.png (toggle the theme if a control exists, else emulate prefers-color-scheme dark via browser_run_code_unsafe page.emulateMedia), 04-mobile-home.png at 390x844, 05-learn.png, 06-any-course-or-subject.png, 07-command-palette.png (press Meta+K). Also capture the header wordmark zoomed (browser_take_screenshot with the element that contains the WoboLogo). Then check the PWA manifest at http://localhost:5177/manifest.webmanifest and record name/short_name/icons. Stop the dev server (kill the background process) when done. If the server cannot start within 3 minutes, report the log tail and stop. Return the list of screenshot paths and the manifest fields in notes.`, { label: 'proof:screens', phase: 'Proof', schema: RESULT, model: 'sonnet', effort: 'medium' })

return { rename: { summary: rename.summary, moved: rename.moved, counts: rename.counts, leftovers: rename.leftovers }, refine: refinedOk.map(r => ({ summary: r.summary, notes: r.notes, leftovers: r.leftovers })), gate: gate ? { summary: gate.summary, gates: gate.gates, leftovers: gate.leftovers, notes: gate.notes } : null, proof: proof ? { summary: proof.summary, notes: proof.notes, changed_files: proof.changed_files } : null }
