export const meta = {
  name: 'wobo-content-parallel',
  description: 'Content and copy that only creates new files: the global framework registry, first-board syllabus fixtures, legal drafts, help centre + About + email copy system, festival/hospitality calendar, platforms readiness — running beside the identity rename',
  phases: [
    { title: 'Content', detail: 'six independent writers, new files only' },
    { title: 'Review', detail: 'one reviewer per deliverable, adversarial' },
    { title: 'Fix', detail: 'writers apply review findings' },
  ],
}
const REPO = args.repo, SCRATCH = args.scratch
const COMMON = `Repo: ${REPO} (branch the-life). ANOTHER WORKFLOW IS RENAMING PACKAGES RIGHT NOW (@classess → @wobo, classess_gateway → wobo_gateway). Therefore: you may ONLY create new files under the paths you are given; never edit an existing file; never import from the gateway or the JS packages; never run git commit/push; never run the repo's test suites (they are mid-rename). Read docs/WOBO-PLAN.md (§14 growth incl. hospitality intent, §15 every element earns its place, §16 Brilliant floor, §17 white-label, §18 device agnostic and handcrafted, §19 Wobo has no gender: name first, they/them only when unavoidable, Wobo says "I"), DESIGN.md (sentence case, no emoji, no exclamation marks, one pigment ultramarine, Poppins + Caveat), docs/CURRICULUM.md. White-label: no provider, model or vendor names anywhere in what you write, beyond "third-party AI and infrastructure providers" in legal text. Product name Wobo; domain heywobo.com; company placeholder [Company legal name], address placeholder [postal address], to be filled by the owner. Return ONLY the structured output.`
const RESULT = { type: 'object', required: ['summary', 'created_files', 'notes'], properties: { summary: { type: 'string' }, created_files: { type: 'array', items: { type: 'string' } }, open_issues: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } }
const REVIEW = { type: 'object', required: ['verdict', 'findings'], properties: { verdict: { type: 'string', enum: ['ship', 'fix-first'] }, findings: { type: 'array', items: { type: 'object', required: ['file', 'severity', 'finding', 'fix'], properties: { file: { type: 'string' }, severity: { type: 'string', enum: ['blocker', 'major', 'minor'] }, finding: { type: 'string' }, fix: { type: 'string' } } } }, notes: { type: 'string' } } }

const JOBS = [
  { key: 'registry', prompt: `${COMMON}\nYOU write the GLOBAL FRAMEWORK REGISTRY under content/curriculum/ (new dir): frameworks.seed.json, README.md, build.py (a standalone script with no repo imports; uses only the standard library; verifies each entry's official_site resolves with an HTTP HEAD/GET and writes verified_site true/false + checked_at into the JSON). Per docs/CURRICULUM.md §3: every Indian national board (CBSE, CISCE with ICSE+ISC, NIOS), every Indian state and UT board with official names and common aliases, IB (PYP/MYP/DP), Cambridge (Primary, Lower Secondary, IGCSE, O Level, AS/A Level), Pearson Edexcel, College Board AP, every US state standards body, England, Scotland, Wales, Northern Ireland, every Australian state/territory + ACARA, every Canadian province/territory, Singapore MOE, Hong Kong, South Africa (CAPS, IEB), UAE MoE and the GCC boards Indian families abroad follow, Nepal, Sri Lanka, Bangladesh, Pakistan (federal + provincial), and common homeschool programmes. Fields: id (slug), name, aliases[], kind (national|state|international|open|homeschool|online), country (ISO 3166-1 alpha-2), region, languages[], levels[] (grades/classes 4–13 as that framework names them), official_site (a real URL you are confident in; never invented), status ('verified' only after build.py confirms the site). RUN build.py so the file carries real check results. Honesty over count; hundreds of entries expected. Aliases must be unique across the file; ids unique.` },
  { key: 'syllabi', prompt: `${COMMON}\nYOU write the FIRST-BOARD SYLLABUS FIXTURES under content/curriculum/syllabi/ (new dir), one JSON per framework+level+subject, per docs/CURRICULUM.md §2 and §5: CBSE classes 6–12 (Mathematics, Science, Social Science; 11–12 Physics, Chemistry, Biology, Mathematics), ICSE 6–10 and ISC 11–12 core subjects, Telangana, Andhra Pradesh, Maharashtra and Karnataka state boards 6–10 core, NIOS secondary and senior secondary core. Each file: framework_id, version (academic year, e.g. 2026-27), level, subject, units[] with title, order, topics[] (title, order, objectives[] where the source states them), and source_ref per unit (document URL + page/section) that you ACTUALLY fetched with curl/python (standard library only) — record fetched_at and a sha256 of the document text you extracted. Where you cannot fetch the official document, mark the file status 'provisional' and say why in a 'note'; never invent a unit. Write a README.md listing every file with its status and source. Aim for complete verified coverage of CBSE first, then the rest.` },
  { key: 'legal', prompt: `${COMMON}\nYOU draft the LEGAL SET under docs/legal/ (new dir), diplomatic in tone, written for a school-age audience's parents and for the learner where the law allows, with an "in plain words" summary box atop each: terms-of-service.md, privacy-policy.md (GDPR, UK GDPR, India DPDP Act 2023 incl. verifiable parental consent for under-18s, COPPA for US under-13s, CCPA/CPRA; categories of recipients incl. "third-party AI and infrastructure providers" without naming any; retention; rights; DPO contact placeholder), childrens-privacy.md, parental-consent.md (the consent flow text and the parent's rights), cookies.md, acceptable-use.md, refund-and-cancellation.md (India RBI auto-renewal disclosure norms, EU 14-day right of withdrawal for digital content, US auto-renewal law notice patterns, Apple/Google in-app purchase caveats), accessibility-statement.md, safety-and-content.md (what Wobo does when a learner is in distress; what content is refused), community-and-flags.md, and README.md with a lawyer-review checklist and every jurisdiction-specific line tagged [REVIEW]. Plain English, sentence case headings, no legalese where a sentence will do, and no promise the product cannot keep (read docs/WOBO-PLAN.md to know what it does).` },
  { key: 'copy', prompt: `${COMMON}\nYOU write the COPY SYSTEM under docs/copy/ (new dir): (1) help-centre/ — three groups (wobo-basics, product-features, boards-and-curriculum) with 8–12 short articles each as Markdown, written for Wobo's real features per docs/WOBO-PLAN.md and docs/BOARD.md (board on screen, the plane, the full board, ask about anything, flags, parent link, plans, settings, curriculum choice and own syllabus), each with a one-line answer first then detail; (2) about.md — mission, how Wobo teaches (drawn live), what we cover, our promises (privacy, honesty, no manipulation of minors), team placeholder; (3) emails/ — the transactional and hospitality email set as Markdown specs with subject lines, preview text, body copy in Wobo's voice, and the variables each needs: welcome, verify-email, password-reset, first-lesson-done, first-week, unit-finished, boss-beaten, streak-milestones, streak-saved, quiet-week check-in, before-exam, parent-weekly-summary, parent-link-invite, plan-confirmation, receipt, renewal-reminder (30 days, 7 days), payment-failed, cancel-thanks, win-back (30/90 days), flag-received, flag-fixed, festival wishes (a template with the festival name and a one-line drawn-greeting description), birthday; (4) voice.md — the one-page voice guide for every writer (name first, they/them only when unavoidable, sentence case, no emoji, no exclamation marks, warm, short, specific, "Hey Wobo" written as two words). Every line obeys §19.` },
  { key: 'festivals', prompt: `${COMMON}\nYOU build the HOSPITALITY CALENDAR under content/hospitality/ (new dir): festivals.json — for each of India (national + by state/region and religion), UAE and GCC, UK, US, Canada, Australia, Singapore, Hong Kong, South Africa, Nepal, Sri Lanka, Bangladesh, Pakistan: the festivals and observances a school-age learner's family would celebrate, with id, name, aliases, country/regions, calendar type (gregorian|lunar|lunisolar|islamic|other), dates for 2026 and 2027 (exact where fixed; computed where lunar, with the computation source named — e.g. official government holiday lists, which you must fetch and cite with URLs), greeting_style (what a warm one-line wish looks like, no emoji), school_holiday (bool), exam_season hints per board (CBSE board exams Feb–Mar, pre-boards, etc.), and quiet_days (days we never market, e.g. days of mourning). Plus school-calendar.json: per major board, the academic year start, term breaks, board exam windows, result days (fetched from official sources with URLs; mark unknown as null, never invent). Include a README.md explaining fields and sources. Standard-library Python only if you script anything.` },
  { key: 'platforms', prompt: `${COMMON}\nYOU write docs/PLATFORMS.md (new file): a readiness audit and plan for shipping the same web app as native apps with Capacitor (iOS + Android phones and tablets) and Tauri v2 (macOS, Windows, Linux), based on reading apps/web-pwa (Vite 6, React 19, PWA plugin, Web Audio/voice WebSockets, camera/file upload, Pyodide, SVG board, hold-to-talk hotkey, localStorage/IndexedDB), packages/wobo, and the gateway contract in DEPLOY.md. Cover: what already works unchanged; what needs a native bridge (microphone permissions, background audio, push notifications, secure storage for the session token, deep links for parent-link and email CTAs, in-app purchases vs web checkout rules on iOS/Android and how the legal text must differ, app-store review risks for AI + minors, COPPA/age-gate flows, offline caching), store listing requirements (icons, screenshots at required sizes, privacy nutrition labels, data-safety form), the build pipeline per platform with the exact commands, code signing and notarisation, update strategy (Capacitor live updates vs store, Tauri updater), and a task list ready to paste into docs/WOBO-TASKS.md Wave 8. Verify library versions against the official docs fetched now, not memory, and cite them.` },
]

const results = await pipeline(
  JOBS,
  j => agent(j.prompt, { label: `write:${j.key}`, phase: 'Content', schema: RESULT, model: 'opus', effort: 'high' }),
  (r, j) => r ? agent(`${COMMON}\nYOU are the REVIEWER for the '${j.key}' deliverable. The writer reported: ${JSON.stringify({ summary: r.summary, files: r.created_files, notes: r.notes, open_issues: r.open_issues })}. Read every created file. Be adversarial: invented URLs or sources (fetch a sample and check), invented syllabus units, wrong dates, legal claims the product cannot keep, provider/vendor names, pronouns for Wobo, exclamation marks/emoji, title case headings, generic filler, missing coverage the brief demanded. Verdict 'ship' only if no blocker/major remains.`, { label: `review:${j.key}`, phase: 'Review', schema: REVIEW, model: 'opus', effort: 'high' }).then(v => ({ write: r, review: v })) : null,
  (x, j) => (x && x.review && x.review.verdict === 'fix-first') ? agent(`${COMMON}\nYOU are the WRITER for '${j.key}' again, fixing review findings in place (you may edit the files YOU created under your paths; still never edit any other existing file). Findings: ${JSON.stringify(x.review.findings)}. Fix every blocker and major; minors where cheap. Report what changed.`, { label: `fix:${j.key}`, phase: 'Fix', schema: RESULT, model: 'opus', effort: 'medium' }).then(f => ({ ...x, fix: f })) : x,
)

return { deliverables: results.map((x, i) => x ? { key: JOBS[i].key, summary: x.write.summary, files: x.write.created_files, verdict: x.review && x.review.verdict, findings: x.review && x.review.findings.filter(f => f.severity !== 'minor').map(f => `${f.severity}: ${f.file}: ${f.finding}`), fixed: x.fix ? x.fix.summary : null, open_issues: x.write.open_issues } : { key: JOBS[i].key, failed: true }) }
