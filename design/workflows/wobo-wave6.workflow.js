export const meta = {
  name: 'wobo-wave6-curriculum',
  description: 'Wave 6: the universal curriculum — registry of boards, versioned ontology in Supabase, discovery job, own-syllabus intake, editable overlay, concept mapping, on-demand generation; gate, adversary, fix',
  phases: [
    { title: 'Foundations', detail: 'schema+API, discovery, own syllabus+concepts, domain' },
    { title: 'Integration', detail: 'seed registry hardening, client swap off the static catalog, goldens' },
    { title: 'Gate', detail: 'full gates + live proofs' },
    { title: 'QA', detail: 'curriculum adversary' },
    { title: 'Fix', detail: 'close findings, re-gate' },
  ],
}
const REPO = args.repo, SCRATCH = args.scratch
const COMMON = `Repo: ${REPO} (Bun workspaces + uv Python; branch the-life). This is WAVE 6 of docs/WOBO-PLAN.md — the universal curriculum. READ FIRST, fully: docs/CURRICULUM.md (law: ontology, registry, discovery, provenance labels, overlay, concept graph, on demand, storage, honesty), docs/WOBO-PLAN.md §4, §9 (model tiers), §13 and §15, docs/WOBO-TASKS.md §6, and infra/supabase/README.md + migrations 0002/0005/0006/0007 for how this repo does Supabase (schema per plane, RLS per subject, service role for writes, PostgREST with Accept-Profile). Laws that bind you: no syllabus without a source; nothing edited in place after publication (new versions only); the learner's overlay survives upgrades; nothing generated in bulk; honest labels; refuse rather than invent; the client never holds a key or a model id; grades 4–13, school level only; product copy sentence case, no emoji, no exclamation marks. Model routing goes through the gateway's tiers (generate = Terra, verify = Opus 5, tiny = Luna). Never run git commit/push. Re-read a file immediately before editing; other workers edit other files concurrently — respect ownership. Tests for everything with logic; keep every suite green. Return ONLY the structured output.`
const RESULT = { type: 'object', required: ['summary', 'changed_files', 'gates', 'api', 'notes'], properties: { summary: { type: 'string' }, changed_files: { type: 'array', items: { type: 'string' } }, gates: { type: 'object', additionalProperties: { type: 'string' } }, api: { type: 'string' }, open_issues: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } }

phase('Foundations')
const f = await parallel([
  () => agent(`${COMMON}\nYOU are WORKER 1 — schema, storage and the curriculum API. You OWN services/gateway/src/wobo_gateway/curriculum/** (new package: models.py, store.py, api.py, labels.py, versions.py, overlay.py), infra/supabase/migrations/0008_curriculum.sql (new; the orchestrator applies it — do NOT run it), services/gateway/src/wobo_gateway/app.py ONLY to mount the curriculum capabilities, registry.py ONLY to add capability policies, budget.py ONLY to classify them, and services/gateway/tests/test_curriculum*.py. Build to CURRICULUM.md §2, §5, §6, §8, §10: the 'curriculum' schema (frameworks, versions, nodes with kind/parent_id/order/aliases/source_ref, provenance, concept_map, overlays keyed by subject_id + version_id with a JSON patch, discovery_jobs state machine, review_queue) with RLS (public rows readable by authenticated; personal frameworks and overlays per subject; writes only by service role through the brain) and indexes for type-ahead; a store over PostgREST with the service-role key (Accept-Profile: curriculum) plus an in-memory store for mock/tests; capabilities: curriculum.search (type-ahead over names+aliases with country hint), curriculum.framework (levels/subjects), curriculum.units (on demand: from stored syllabus, else enqueue discovery and return a provisional placeholder with status), curriculum.topics, curriculum.pin/upgrade (version pinning + diff), curriculum.overlay.get/apply (JSON patch add/remove/reorder/rename/not_in_my_school/attach_textbook, re-applied on upgrade with a report), curriculum.status; labels.py maps status → the four honest labels; versions.py enforces immutability (a correction is a new version with supersedes). Define load_seed() yourself in store.py: it reads content/curriculum/frameworks.seed.json (a parallel writer produced it; tolerate missing/extra fields and skip malformed entries with a logged count) so the store works before Worker 2 hardens it. Everything authenticated and budgeted like other capabilities (search is tiny-tier and cheap; nothing here calls a model except discovery, which Worker 3 owns). Tests against the in-memory store and a PostgREST fake.`, { label: 'f1:schema-api', phase: 'Foundations', schema: RESULT, model: 'opus', effort: 'high' }),
  () => agent(`${COMMON}\nYOU are WORKER 3 — the discovery job. You OWN services/gateway/src/wobo_gateway/curriculum/discovery/** (new: search.py, fetch.py, extract.py, verify.py, job.py, freshness.py) and services/gateway/tests/test_discovery*.py; you may add pure-Python deps to services/gateway/pyproject.toml (pypdf or pdfplumber for PDFs; a small HTML-to-text; nothing heavy) and run uv sync --all-packages. Build CURRICULUM.md §4 and §9: search.py behind an interface with a mock implementation for tests and one real provider chosen from what the keys support (prefer the provider's native web-search tool via the router — e.g. the OpenAI search-capable model or Anthropic's web search tool — over a new third-party key; document the choice and make it env-switchable); fetch.py (HTML and PDF → text with page anchors, size and time budgets, robots-respecting, no JS execution); extract.py (the generate tier turns the document into the schema as strict JSON with source_ref per node; retries once on schema failure); verify.py (the verify tier re-reads the source and the extraction; structural checks in code: unit counts vs the document's table of contents, level coverage, duplicates, empties, ordering; a failed check redraws once then refuses); job.py (state machine queued→searching→fetching→extracting→checking→provisional|refused with budgets, idempotency per framework+level+subject so a second learner never triggers a second discovery, and the promotion rule: verified after checks pass and two distinct learners used it without structural edits, or owner review); freshness.py (monthly re-fetch + release-window schedule, hash compare, new version on change, a diff summary for Wobo to say). Everything metered as a generation. Tests with fixture documents (one CBSE-style PDF text, one HTML syllabus) covering success, refusal, idempotency, and the honest failure path.`, { label: 'f3:discovery', phase: 'Foundations', schema: RESULT, model: 'opus', effort: 'high' }),
  () => agent(`${COMMON}\nYOU are WORKER 4 — own syllabus intake and the concept graph. You OWN services/gateway/src/wobo_gateway/curriculum/{own.py,concepts.py} and services/gateway/tests/test_curriculum_own.py, test_curriculum_concepts.py; read-only elsewhere. own.py: intake of a learner's own syllabus as paste/typed text, a photo (image → text via the image-capable tier), or a PDF → the generate tier structures it into a personal framework (kind: personal, owner subject) with per-unit confirmation flags; optional offer to the registry as community (moderated: goes to review_queue). concepts.py: map topics onto the canonical concept graph (the existing prerequisite graph in packages/sdk / content; find it) — propose mappings with the generate tier, confirm with structural checks (level band, objective overlap), store in concept_map with confidence; expose concept_ids on topics so content caches key by concept. Tests with fixtures.`, { label: 'f4:own+concepts', phase: 'Foundations', schema: RESULT, model: 'opus', effort: 'medium' }),
  () => agent(`${COMMON}\nYOU are WORKER 5 — the domain. The owner bought heywobo.com (2026-09-03). Web = https://heywobo.com (www redirects), gateway = https://api.heywobo.com (Railway custom domain, being attached), email from hello@heywobo.com and no-reply@heywobo.com, support@heywobo.com. You OWN: vercel.json (CSP connect-src/media-src/img-src: replace the Railway host with api.heywobo.com and keep the Supabase host ONLY if no proxy; add Vercel rewrites so the browser talks to the database through our own origin — /db/* → the Supabase project URL — and make the web client's Supabase URL configurable to https://heywobo.com/db when VITE_SUPABASE_PROXY=1; verify auth + PostgREST work through the rewrite in a unit test of the URL builder), apps/web-pwa/.env.production (VITE_APP_URL=https://heywobo.com, VITE_GATEWAY_URL=https://api.heywobo.com, VITE_APP_NAME=Wobo), apps/web-pwa/index.html + PWA manifest (start_url, id, canonical, og:url), DEPLOY.md (env contract with the real values; a 'DNS records' table: apex A/ALIAS → Vercel, www CNAME, api CNAME → Railway target, SES DKIM CNAMEs, SPF TXT, DMARC TXT, plus the Supabase note), infra/dns/heywobo.com.md (the same table as the single source of truth), services/gateway/src/**/email.py + email_templates.py DEFAULTS ONLY (EMAIL_FROM 'Wobo <hello@heywobo.com>', reply-to support@, unsubscribe URL under heywobo.com, postal address placeholder clearly marked), services/gateway/src/**/config or settings module APP_URL default, sitemap/robots with the domain. Do not touch app.py or anything under curriculum/**. Nothing may still reference up.railway.app or vercel.app in shipped code (tests may). Gates: cd ${REPO} && bun run typecheck && bunx biome check --write apps/web-pwa && bun run test && cd apps/web-pwa && bun run build && cd ${REPO} && uv run pytest -q services/gateway && python3 -c "import json;json.load(open('vercel.json'))" && grep -rIl -E 'up\\.railway\\.app|vercel\\.app' apps packages services --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.venv | grep -v test | wc -l (must be 0).`, { label: 'f5:domain', phase: 'Foundations', schema: RESULT, model: 'opus', effort: 'medium' }),
])
const found = f.filter(Boolean)
const APIS = found.map((r, i) => `--- worker ${i + 1} API ---\n${r.api}`).join('\n')

phase('Integration')
const integ = await parallel([
  () => agent(`${COMMON}\nYOU are WORKER 2 — the seed registry. A PARALLEL WRITER may still be finishing content/curriculum/**. Before touching it: check that content/curriculum/README.md exists and that no file under content/curriculum changed in the last 3 minutes (compare mtimes twice, 90 s apart, with a shell loop; wait up to 60 minutes total). Only then read and extend. A parallel writer has ALREADY produced content/curriculum/frameworks.seed.json, README.md and build.py (reviewed). Start by reading them; your job is to VALIDATE and EXTEND, not rewrite: fix schema drift, fill gaps against the list below, re-run build.py, and write the loader + tests. You OWN content/curriculum/** and services/gateway/tests/test_curriculum_seed.py. Produce the global registry of frameworks per CURRICULUM.md §3: every Indian national board (CBSE, CISCE/ICSE+ISC, NIOS) and every state board (all states and UTs with their official board names and common aliases), IB (PYP, MYP, DP), Cambridge (Primary, Lower Secondary, IGCSE, O Level, AS/A Level), Pearson Edexcel (International GCSE, A Level), College Board AP, every US state (their K-12 standards body), England, Scotland, Wales, Northern Ireland, every Australian state and territory + ACARA, every Canadian province/territory, Singapore MOE, Hong Kong, South Africa (CAPS/IEB), UAE (MoE) and the GCC boards commonly followed by Indian families abroad, plus the common homeschool programmes (kind: homeschool). For each: id (slug), name, aliases[], kind (national|state|international|open|homeschool|online), country (ISO), region, languages[], levels[] (the grade/class range 4–13 as the framework names them), official_site (the real URL; verify it resolves with a HEAD/GET request from a script — no invented URLs; if a site cannot be verified mark verified_site:false), status: 'verified' only when the site check passed. Draft with care, then VERIFY every entry's official site by actually fetching it (content/curriculum/build.py does the check and writes the result); anything unverifiable stays with a note. Aim for completeness (several hundred entries) and honesty over count. Provide a loader Worker 1's store can import (the API surface: load_seed() -> list[Framework]). Tests: schema validity, uniqueness of ids and aliases, every Indian state present, every verified entry has a resolvable site (mocked in tests, real in build.py).`, { label: 'i2:seed', phase: 'Integration', schema: RESULT, model: 'opus', effort: 'high' }),
  () => agent(`${COMMON}\nAPIs from the foundation workers:\n${APIS}\nYOU are WORKER 5 — the client. You OWN apps/web-pwa/src/data/**, apps/web-pwa/src/screens/{Learn.tsx,SubjectScreen.tsx,You.tsx,Onboarding.tsx,FrameBuilding.tsx}, apps/web-pwa/src/screens/home/stops.ts, packages/sdk/src/** (curriculum client), and new apps/web-pwa/src/curriculum/** . Replace the static catalog (apps/web-pwa/src/data/catalog.ts) and the "frame" system with on-demand fetch from the brain (plan §13: do not patch the frame; replace it): a type-ahead board search (registry via curriculum.search, country hint from locale, "not listed? tell me" always visible → triggers discovery with an honest status card; "show me my syllabus" → own-syllabus intake: paste, photo via camera/upload, PDF), level and subject pickers from the framework, units on open (with the provisional placeholder and status when discovery is running), topics on open, provenance labels in plain language, the overlay editor (add, remove, reorder, rename, not in my school, attach a textbook) with edits surviving a version upgrade (diff card from curriculum.pin/upgrade), offline cache of the pinned version, and grades 4–13. Keep Learn/Subject/You UI minimal and honest (these screens get their design raise in Wave 7); Home stops read from the new source, never inventing a syllabus (the audit's "invented CBSE Class 8" must be impossible: empty world → one honest stop). Delete catalog.ts and frame.ts once nothing imports them. Tests: sdk client, search debounce, overlay patch round-trip, no-invention.`, { label: 'i5:client', phase: 'Integration', schema: RESULT, model: 'opus', effort: 'high' }),
  () => agent(`${COMMON}\nAPIs from the foundation workers:\n${APIS}\nYOU are WORKER 6 — goldens and seeding the first boards. A PARALLEL WRITER may still be finishing content/curriculum/syllabi/**. Before touching it: check that content/curriculum/README.md exists and that no file under content/curriculum changed in the last 3 minutes (compare mtimes twice, 90 s apart, with a shell loop; wait up to 60 minutes total). Only then read and extend. A parallel writer has ALREADY produced content/curriculum/syllabi/** with a README listing each file's status (verified/provisional) and source; read it first and VALIDATE/EXTEND rather than rewrite (re-fetch a sample of sources; fix drift against Worker 1's schema; fill gaps). You OWN services/gateway/tests/test_curriculum_golden.py, apps/web-pwa/tests/curriculum*.spec.ts, and content/curriculum/syllabi/**. Author, from the official documents, verified syllabus fixtures for the first boards at the unit level: CBSE classes 6–12 (Mathematics, Science, Social Science; 11–12 Physics/Chemistry/Biology/Maths), ICSE 6–10 + ISC 11–12 core, Telangana/Maharashtra/Karnataka/AP state boards 6–10 core, NIOS secondary and senior secondary core — each unit with source_ref (document URL + page/section) that you actually fetched; where you cannot fetch the official document, mark the fixture provisional and say so; never invent. Load them through Worker 1's store as verified/provisional accordingly. Golden tests: search finds each board by name and alias; units for a level+subject return the fixture with the right label; a request for an unseeded board+level enqueues exactly one discovery job; the overlay survives a version bump; grades 4–13 boundaries. Playwright: pick a board via type-ahead, open a subject, see honest labels; "not listed" path shows the status card; own-syllabus paste builds a personal plan.`, { label: 'i6:goldens', phase: 'Integration', schema: RESULT, model: 'opus', effort: 'medium' }),
])
const integd = integ.filter(Boolean)

phase('Gate')
const gate = await agent(`${COMMON}\nWorkers reported: ${JSON.stringify([...found, ...integd].map(r => ({ summary: r.summary, gates: r.gates, open_issues: r.open_issues })))}. YOU are the GATE; every file is yours. Run from ${REPO}: bun install --frozen-lockfile || bun install; bun run typecheck; bunx biome check --write . && bunx biome check .; bun run test; cd apps/web-pwa && bun test test/*.test.ts src && bun run build && bunx playwright test --reporter=list; cd ${REPO} && uv sync --all-packages && uv run pytest -q services/gateway && uv run pytest -q services/verifier && uvx ruff check services content. Fix every failure at the root. LIVE PROOFS (mock mode, gateway on 8081 with DEV_AUTH=1, web on 5185): screenshots to ${SCRATCH}/proof/: board type-ahead with "tel" → Telangana; CBSE Class 9 Mathematics units with the verified label; an unseeded board's status card; own-syllabus paste → personal plan; the overlay editor after removing a unit; dark theme of the units list. Kill servers. Return gates and the proof list.`, { label: 'gate', phase: 'Gate', schema: RESULT, model: 'opus', effort: 'high' })

phase('QA')
const qa = await agent(`${COMMON}\nYOU are the CURRICULUM ADVERSARY (read-only; write under ${SCRATCH}/qa). The gate reported: ${JSON.stringify({ summary: gate?.summary, notes: gate?.notes })}. Try to break the laws: make her invent a syllabus (unseeded board, discovery failing, empty search); find any node without provenance; find an in-place edit path; lose an overlay across an upgrade; trigger a second discovery for the same framework+level+subject (race two learners); leak the service-role key or a model id to the client; get a personal framework of one learner read by another (RLS); overstate a label; bulk-generate; a grade outside 4–13; alias collisions in the seed; unverified official sites marked verified. Reproduce with scripts/tests; list file:line, severity, minimal fix.`, { label: 'qa', phase: 'QA', schema: RESULT, model: 'opus', effort: 'high' })

phase('Fix')
const fix = await agent(`${COMMON}\nYOU are the FIXER; every file is yours. QA reported: ${JSON.stringify({ notes: qa?.notes, open_issues: qa?.open_issues })}. Fix every real finding at the root with a regression test, re-run all gates and the live proofs, refresh ${SCRATCH}/proof/. Report anything deliberately left and why.`, { label: 'fix', phase: 'Fix', schema: RESULT, model: 'opus', effort: 'high' })

return { foundations: found.map(r => ({ summary: r.summary, gates: r.gates, open_issues: r.open_issues })), integration: integd.map(r => ({ summary: r.summary, gates: r.gates, open_issues: r.open_issues })), gate: gate ? { summary: gate.summary, gates: gate.gates, notes: gate.notes, open_issues: gate.open_issues } : null, qa: qa ? { notes: qa.notes, open_issues: qa.open_issues } : null, fix: fix ? { summary: fix.summary, gates: fix.gates, notes: fix.notes, open_issues: fix.open_issues } : null }
