export const meta = {
  name: 'wobo-wave9-law-v5',
  description: 'Wave 9: law v5 everywhere — the new white-ground homepage ported into the app, the seven other public pages and every app screen moved onto the white token set, the copy law applied product-wide (no names, no grade gate, no raw allowances, location inferred, promote before invite), and the three causes of scroll jitter removed wherever they occur; gated by every repo gate, the responsive harness, a jitter audit and an adversarial visual QA',
  phases: [
    { title: 'Tokens', detail: 'one worker: the white token set + the jitter rules in the kit' },
    { title: 'Build', detail: 'landing port; public pages; app screens; the copy sweep' },
    { title: 'Gate', detail: 'gates + harness + jitter audit + visual QA against the prototype' },
    { title: 'Fix', detail: 'close findings, re-gate' },
  ],
}
const REPO = args.repo, SCRATCH = args.scratch, PROTO = args.proto
const COMMON = `Repo: ${REPO} (branch the-life; Bun workspaces @wobo/*; Vite/React PWA at apps/web-pwa; kit at apps/web-pwa/src/ui/{tokens.css,primitives/**}; landing at apps/web-pwa/src/screens/landing/** with its own engine/**; public pages at screens/site/**, screens/pitch/**, screens/legal/**, screens/contact/**, screens/plans/**).

READ FIRST: DESIGN.md §0 "Law v5". It supersedes the cream ground of v3/v4 and carries the spacing rhythm, the three causes of scroll jitter, and the copy law. The built expression of the law is ${PROTO}/landing-v8.html — Fable's hand, handcrafted for this wave. Copy its token block, its spacing variables, its motion rules and its COPY VERBATIM. Frames beside it: v8-{d,t,m}-NN.png at 1440/834/390.

THE LAW, in short, because every finding in this wave traces to one of these:
1. WHITE ground (#FFFFFF light, #0E0E16 night). No tinted section backgrounds anywhere. Colour only where it does a job: pig points, marigold marks, mint confirms, rose cares.
2. NO NAMES. Not "Aanya", not any invented learner or parent, anywhere in product copy, prototypes, emails, tests or fixtures a reader could see. Say "your child", "the learner", or address the reader.
3. NO GRADE GATE. Never "classes 4 to 12" or any age range on a public surface. "Every subject your board sets."
4. NO RAW ALLOWANCES. Never "40 questions a day". Free carries no multiplier; Pro is "five times the free allowance"; Max is "twenty times".
5. LOCATION IS INFERRED. No country switch on Plans or anywhere else: read the browser time zone (or the request country server-side) and show that currency.
6. PROMOTE BEFORE INVITE. The closing call is early access, not "begin tonight".
7. DRAWING IS ONE PART. No surface may imply the board is the whole product.
8. JITTER: one owner per animated property (never a CSS transition on a property GSAP scrubs); transform and opacity only (never scrub width/height/top); fire tweens on state change, never inside onUpdate. A magnetic control moves an INNER element, never itself.
9. Spacing: --gutter clamp(20px,5vw,48px), --band clamp(72px,9vw,132px) applied as half from each side, --colgap clamp(32px,5vw,80px); every grid child min-width:0; no horizontal scroll at 390/834/1440.
Also standing: Wobo has no gender (§19), neutral by default (§20), white-label (§17, never name a vendor or model).

Do not run git commit. Return the structured result.`
const RESULT = { type: 'object', required: ['summary', 'changed_files', 'gates', 'open_issues'], properties: { summary: { type: 'string' }, changed_files: { type: 'array', items: { type: 'string' } }, gates: { type: 'object' }, open_issues: { type: 'array', items: { type: 'string' } } } }
const FINDINGS = { type: 'object', required: ['summary', 'findings', 'gates'], properties: { summary: { type: 'string' }, gates: { type: 'object' }, findings: { type: 'array', items: { type: 'object', required: ['where', 'severity', 'what'], properties: { where: { type: 'string' }, severity: { type: 'string', enum: ['blocker', 'major', 'minor'] }, what: { type: 'string' }, fix_hint: { type: 'string' } } } } } }

phase('Tokens')
const kit = await agent(`${COMMON}\nYOU are WORKER A — THE TOKEN SET AND THE MOTION RULES. You OWN apps/web-pwa/src/ui/tokens.css, ui/tokens.ts, ui/primitives/ui.css and their tests, plus apps/web-pwa/index.html's first-paint colour and vite.config.ts's PWA manifest colours. Replace the palette-v4 cream token block with law v5's white set, character for character from the :root and [data-theme="dark"] blocks at the top of ${PROTO}/landing-v8.html, and add the spacing variables (--gutter, --band, --colgap). Keep every token NAME that the app already uses so no screen breaks; change only the values, and add the new ones. Where a screen used a tinted surface as a section ground, the token it reads must now resolve to white or paper-2 — go through LEGACY_TOKEN_BRIDGE and repoint every cream value. First paint and theme-color become #FFFFFF light / #0E0E16 dark. Then add to ui.css the two motion rules the law demands as reusable classes: .wk-mag (a magnetic control whose INNER span moves — copy the implementation from the prototype's script comment) and a documented rule that no .wk- class may put a CSS transition on transform or opacity that GSAP also drives. Update ui.test.ts / tokens.test.ts to assert the v5 values and to FAIL if any cream hex (#FAF7F0, #F1EDE3, #E7E1D3, #0F1226, #181C3A) appears in the app's source. Gates: bun run typecheck, bunx biome check ., bun run test, bun run build, bun run gate.`, { label: 'A:tokens', phase: 'Tokens', schema: RESULT })

phase('Build')
const built = (await parallel([
  () => agent(`${COMMON}\nTokens: ${JSON.stringify(kit ? kit.summary : 'token worker returned nothing — take the values from the prototype yourself')}.\nYOU are WORKER B — THE HOMEPAGE. You OWN apps/web-pwa/src/screens/landing/** (page, sections, engine, copy, styles) and its tests. REPLACE the current landing with the port of ${PROTO}/landing-v8.html: white ground; the simple header (How it works · Subjects · For parents · For students · Plans, each a REAL route, plus Sign in and a magnetic "Get early access"); the hero whose card answers one question four ways (Drawn / Filmed / Tried / Spoken) with the drawn answer already complete on first paint and a rail that cycles and is clickable; the five-step loop; the pinned four-form sequence; the film that plays, pauses, is circled and is answered; the practice moment with the ring and the burst; the parent report with counting numbers, growing bars, the projection line and the badges; the subject families (never grades); the six safe-by-design items; Ask Wobo plus the row of buttons that hand the question to the reader's own assistant (ChatGPT, Claude, Gemini, Perplexity, Grok — the exact deep links in the prototype); the FAQ; the devices row; and the ink close panel whose call is EARLY ACCESS with an email field. Copy verbatim. Keep the engine's structure but obey law §8: audit every existing tween for the three jitter causes and fix each one you find. The email field posts to the existing contact/waitlist path if one exists, else stores locally and says so honestly. Both themes, 390/834/1440, zero console errors, zero horizontal scroll.`, { label: 'B:homepage', phase: 'Build', schema: RESULT }),
  () => agent(`${COMMON}\nTokens: ${JSON.stringify(kit ? kit.summary : 'take the values from the prototype')}.\nYOU are WORKER C — THE OTHER PUBLIC PAGES. You OWN apps/web-pwa/src/screens/pitch/** (security, meet-wobo, for-parents, for-students, how-it-works, subjects), screens/site/** (shell, about, help), screens/plans/**, screens/contact/**, screens/legal/**. Move every one onto the white ground: no tinted section backgrounds, cards on paper-2, accents only where they do a job. Apply the copy law across all of them: strip every name, every grade range, every raw allowance; Plans loses its country switch and infers the currency from the time zone (the pattern is in ${PROTO}/site-plans.html); the closing call on every page becomes early access. The reference for each page's structure stays its prototype in ${PROTO}/site-*.html — port the corrected copy from those files, which Fable has already updated. Keep the shell, the footer and the Ask Wobo block shared. Both themes, three widths, harness green on every route you own.`, { label: 'C:public-pages', phase: 'Build', schema: RESULT }),
  () => agent(`${COMMON}\nTokens: ${JSON.stringify(kit ? kit.summary : 'take the values from the prototype')}.\nYOU are WORKER D — THE APP SCREENS. You OWN apps/web-pwa/src/screens/{Home,Learn,Course,SubjectScreen,ChatScreen,You,practice,parent,onboarding,states}/** and their stylesheets, plus packages/wobo/src/board/** chrome. Move every authenticated screen onto the white ground: the rail, the cards, the board frame, the practice tiles, the report. A section never gets a tint to mark itself; space and type do that. Colour stays for the pointer, the highlighter, confirmation and care. Apply the copy law: no invented names in any placeholder, sample, empty state or fixture a learner could see; no grade ranges; no raw allowances (the allowance card says what is left in words, not "25 of 40"). Audit every animated surface for the three jitter causes. Both themes, three widths, harness green on every app route.`, { label: 'D:app-screens', phase: 'Build', schema: RESULT }),
  () => agent(`${COMMON}\nYOU are WORKER E — THE COPY SWEEP AND THE CONTENT SURFACES. You OWN docs/copy/**, docs/SITE.md, services/gateway/src/wobo_gateway/email_templates.py, ask_public.py's SUGGESTIONS, and content/hospitality/festivals.json's wish lines. Sweep every one for the copy law: (1) no invented person's name anywhere — the email templates, the help articles, the about page, the ask suggestions, the sample notes; use "your child" or the reader; (2) no grade range on any public surface; (3) no raw allowance numbers; (4) nothing that implies drawing is the whole product — where a line says Wobo draws, make sure at least the neighbouring line names another form (films, simulates, speaks, practises, remembers); (5) the closing call in marketing copy is early access. Then ADD the guard: a test in services/gateway/tests that scans docs/copy/**, the email templates and the ask suggestions and FAILS on a first-name pattern, on "class 4"/"classes 4 to 12"/"grade" ranges, and on a bare "N questions a day". Keep the tests honest — the pattern must not fire on legitimate words. Gates: uv run pytest services/gateway, uvx ruff check services/gateway, bun run gate.`, { label: 'E:copy-sweep', phase: 'Build', schema: RESULT }),
])).filter(Boolean)

phase('Gate')
const gate = await agent(`${COMMON}\nWorkers reported: ${JSON.stringify(built.map(r => ({ summary: r.summary, gates: r.gates, open_issues: r.open_issues })))}.\nYOU are the GATE for Wave 9, and your job is to REFUSE the claim that law v5 has landed. From ${REPO}: bun run typecheck; bunx biome check .; bun run test; bun run build; bun run gate; uv run pytest services/gateway; then the responsive harness at 390/834/1440 in both themes, saving frames to ${SCRATCH}/frames/.
Then four audits, each with evidence:
(a) COLOUR: computed styles on every route — no cream hex anywhere, no tinted section ground (a section's background must be the page ground or transparent), accents only on elements doing one of the four jobs.
(b) JITTER: for every route with scroll motion, list each animated property and its owner; flag any property with both a CSS transition and a GSAP tween, any scrubbed layout property, and any tween created inside onUpdate. Then measure: scroll each page in 40 steps with CDP and report the worst frame time and any frame over 32 ms.
(c) COPY: grep the built dist and the source for any first name, any grade range, any "N questions a day", any country switch, and "begin tonight". Every hit is a finding.
(d) FIDELITY: compare the app's homepage against ${PROTO}/landing-v8.html section by section at all three widths and name every difference in structure, spacing or copy.
Return findings sorted blocker → minor plus the gate outputs.`, { label: 'gate:9', phase: 'Gate', schema: FINDINGS, effort: 'high' })

phase('Fix')
const fix = gate && gate.findings && gate.findings.length
  ? await agent(`${COMMON}\nYOU are the FIXER for Wave 9; you own exactly the files the workers created or changed (${JSON.stringify([...(kit ? kit.changed_files : []), ...built.flatMap(r => r.changed_files)].slice(0, 300))}). The gate found: ${JSON.stringify(gate.findings)}. Close every blocker and major and every minor you can, without inventing copy. Re-run every gate and the harness; re-shoot the frames the findings named into ${SCRATCH}/frames-fixed/. Return what you fixed, what you could not, and the final gate outputs.`, { label: 'fix:9', phase: 'Fix', schema: RESULT })
  : null

return { kit: kit ? { summary: kit.summary, gates: kit.gates, open_issues: kit.open_issues } : null, built: built.map(r => ({ summary: r.summary, gates: r.gates, open_issues: r.open_issues })), gate: gate ? { summary: gate.summary, gates: gate.gates, findings: gate.findings } : null, fix: fix ? { summary: fix.summary, gates: fix.gates, open_issues: fix.open_issues } : null }
