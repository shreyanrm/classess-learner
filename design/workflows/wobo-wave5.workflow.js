export const meta = {
  name: 'wobo-wave5-nervous-system-and-board',
  description: 'Wave 5: surface registry + gesture layer + context packet, the ink renderer/grammar/plane, the ink-visor character rig, brain board-plan streaming, screen integration, golden boards; gate, UX QA, fix',
  phases: [
    { title: 'Foundations', detail: 'four Opus workers on disjoint packages' },
    { title: 'Integration', detail: 'screens register, plane + video handoff, companion modes, golden boards' },
    { title: 'Gate', detail: 'full gates + live board proofs' },
    { title: 'QA', detail: 'adversarial UX/a11y/perf review' },
    { title: 'Fix', detail: 'close findings, re-gate' },
  ],
}
const REPO = args.repo, SCRATCH = args.scratch
const COMMON = `Repo: ${REPO} (Bun workspaces + uv Python; branch the-life). This is WAVE 5 of docs/WOBO-PLAN.md — the product core. READ FIRST, fully: docs/BOARD.md (the board contract: objects, anchors, streaming protocol, three presentations, pipelines, the hand, budgets), docs/WOBO-PLAN.md §1–3 and §13, WOBO.md, DESIGN.md §2–§5 and §12, docs/WOBO-TASKS.md §5.x for your area. Laws that bind you: nothing is placed by pixels, every mark anchors to a registry target, a focus region, another object, or board space; every number on a board is computed by code and verified before it is drawn; the client never holds a key, model id or limit; no provider name reaches the user; product copy is sentence case, no emoji, no exclamation marks; reduced motion is honoured everywhere; keyboard paths exist for every interaction; one hit of pigment (ultramarine) — her eyes and her pen; no shadows; 3 px radius. Existing substrate to build ON, not around: packages/wobo (context-bus.tsx = the scene bus, WoboBody.tsx = the old rig, highlight-overlay.tsx, actions.ts = the action vocabulary), apps/web-pwa/src/wobo (speech.tsx = voice choreography, Companion.tsx = the docked orb, chat.tsx, paths/), packages/sdk/src/gateway.ts (the one door to the brain), services/gateway/src/classess_gateway/wobo.py (the turn), the verifier (services/verifier). Old screens, engines and the composer layer are being rebuilt later (plan §13): do not fix them; only touch them where the new layer needs a hook (registering targets), and keep those hooks minimal. Never run git commit/push. Re-read a file immediately before editing; other workers edit other files concurrently — respect the ownership in your brief. Add tests for everything with logic (bun test for TS, pytest for Python) and keep every existing suite green. Return ONLY the structured output.`
const RESULT = { type: 'object', required: ['summary', 'changed_files', 'gates', 'api', 'notes'], properties: { summary: { type: 'string' }, changed_files: { type: 'array', items: { type: 'string' } }, gates: { type: 'object', additionalProperties: { type: 'string' } }, api: { type: 'string', description: 'the exported API other workers must use: names, signatures, file paths, events, env vars' }, open_issues: { type: 'array', items: { type: 'string' } }, notes: { type: 'string' } } }

phase('Foundations')
const f = await parallel([
  () => agent(`${COMMON}\nYOU are WORKER 1 — the senses: surface registry, gesture layer, focus objects, context packet. You OWN packages/wobo/src/registry.ts, packages/wobo/src/gesture.tsx, packages/wobo/src/focus.ts, packages/wobo/src/packet.ts (new files), packages/wobo/src/index.ts (exports only), their tests, and apps/web-pwa/src/wobo/capabilities.ts + apps/web-pwa/src/wobo/tutor.ts ONLY where the packet must be attached to wobo.turn calls. Build:
- registry.ts: registerSurface({ id, title, description, targets: [{ id, kind, label, rect(), actions: [{ name, description, inputSchema, run }] }] }) returning an unregister; useSurface/useTarget React hooks; stable ids; re-measure on scroll/resize (ResizeObserver + a throttled scroll listener); a snapshot() serialiser producing the "what is on screen" JSON under 2 KB with deterministic truncation rules; shaped like WebMCP's registerTool so a navigator.modelContext adapter is one file later; a dev-only inspector overlay (env flag) that outlines registered targets with their ids.
- gesture.tsx: one transparent layer mounted at the app root capturing text selection, cursor lasso (freehand circle; pointer events), hover-and-hold (700 ms), long-press on touch, two-finger circle on touch, and a desktop hotkey (hold to talk: configurable, default Alt+Space); each produces a FocusObject { id, kind, targetIds[], text, numbers[], rect, ownerState, createdAt } resolved via elementsFromPoint against the registry; a quiet "ask Wobo about this" chip near the focus (keyboard reachable; Escape clears); focus objects announced to screen readers; never a screenshot.
- packet.ts: buildPacket({ focus, registrySnapshot, route, task, mind, turns, budget }) with priority truncation under a token budget (estimate 4 chars/token), deterministic and unit-tested; attach it to every wobo.turn invocation (payload.context.packet) without breaking the existing context fields.
Gates: bun run typecheck; bunx biome check --write packages apps/web-pwa; bun run test; cd apps/web-pwa && bun test test/*.test.ts src && bun run build.`, { label: 'f1:senses', phase: 'Foundations', schema: RESULT, model: 'opus', effort: 'high' }),
  () => agent(`${COMMON}\nYOU are WORKER 2 — the hand: the board grammar, the ink renderer, the three presentations. You OWN packages/wobo/src/board/** (new: schema.ts, anchors.ts, layout.ts, renderer.tsx, pen.ts, handwriting.ts, plane.tsx, fullboard.tsx, timeline.ts, export.ts, index.ts), packages/wobo/src/index.ts (exports only, coordinate by appending), apps/web-pwa/public/fonts/ (add Caveat's OFL-licensed TTF for glyph-to-stroke), and packages/wobo/package.json dependencies (opentype.js is allowed for glyph outlines; nothing else new). Build to BOARD.md §2–§5, §7–§10:
- schema.ts: the object grammar as zod (marks: point, circle, underline, arrow, bracket, strike, number, write, erase, wipe; shapes: line, polyline, curve, polygon, ellipse, axis, grid, table, label, tex, bond, atom, region, image; controls: slider, toggle, input, drag), common fields id/kind/anchor/style/t/depends, anchors ({target}|{object}|{focus}|{board:[x,y]} in a 1000-unit space), the event union (say/ink/action/ask/card/done), and a codegen script that emits the Python mirror to services/gateway/src/classess_gateway/board_schema.py (JSON schema) so the brain validates the same grammar — do NOT edit other gateway files.
- renderer.tsx: one SVG layer; objects drawn along their paths with pen physics (anticipation, overshoot, settle), timing from t.start/t.dur relative to the utterance clock exposed by speech.tsx (read its API; do not modify speech.tsx — Worker 4 owns integration); marker aesthetic in light, chalk in dark via tokens; the pen sound hook (a tiny synthesized tick via WebAudio, muted with the app's mute); fading ink; eraser swipe; reduced-motion draws instantly in order; anchors resolved every frame the anchor's rect changes and on theme change; object memory (re-point/move/fade/redraw by id); learner ink capture on the same layer producing focus objects; 60 fps with 2,000 strokes (virtualise history; transforms only).
- handwriting.ts: Caveat glyph outlines via opentype.js → stroke order heuristics (left to right, top to bottom per contour) → write objects genuinely written, paced to the utterance; fallback to progressive text reveal if the font fails to load.
- tex: render TeX to paths (use the lightest option that works offline; if none is acceptable, KaTeX to HTML inside a foreignObject with a written reveal is the fallback — document the choice).
- plane.tsx: the frosted overlay board sliding in from the orb (Framer Motion springs), drag, resize, pin, minimize to a thumbnail with ink intact, sheet on phones, summon API; fullboard.tsx: full-bleed variant; layout.ts: places objects in board space so nothing collides, label margins, auto-scroll/zoom when full; timeline.ts: scrub history; export.ts: shareable PNG with the wordmark.
- A storybook-free demo route is NOT needed; instead write a test harness page apps/web-pwa/src/wobo/board-bench.tsx reachable only in dev that plays a golden plan (Pythagoras: axes, triangle, squares, the derivation written) so QA can screenshot it.
Gates: bun run typecheck; bunx biome check --write packages apps/web-pwa; bun run test; cd apps/web-pwa && bun test test/*.test.ts src && bun run build.`, { label: 'f2:board', phase: 'Foundations', schema: RESULT, model: 'opus', effort: 'high' }),
  () => agent(`${COMMON}\nYOU are WORKER 3 — the character rig v2. You OWN packages/wobo/src/body/** and its tests, plus apps/web-pwa/public/{pwa-192.png,pwa-512.png,pwa-maskable-512.png,apple-touch-icon.png,favicon.svg} regeneration (via a Playwright or node script you write under ${SCRATCH}/icons; keep the sizes). The owner approved the "ink visor wobot": light theme = near-black body (#1A1A1F, 92% ink) with a white visor; dark theme = paper body (#EDEDF1) with a near-black visor; eyes ALWAYS ultramarine (#1F35E0 light / #7B8CFF dark) — the only pigment on screen; an ultramarine-tipped pen held by a mitt that appears only when drawing; a half-pixel hairline in the opposite tone so she stays legible over any content. The approved prototype with the exact geometry, expressions, idle life and reactions is at ${SCRATCH}/../design/wobo-rig.html — read it and port it faithfully into WoboBody.tsx (keep the existing exported interface and props so every consumer keeps working: moods listening/thinking/explaining/celebrating/resting plus the new ones), as React + Framer Motion springs (or rAF springs if lighter): 20 expressions (idle, listening, thinking, computing, aha with spark, explaining, drawing, celebrating, encouraging, curious, surprised, wink, supportive, bored, sleepy, loading, proud, shy, focused, happy), 15 behaviours (tap, nod, hop, lean, peek, shrink, grow, wiggle, yawn, sigh, shake, point, startle, settle, stretch), eyes that track a target (a focus rect or pointer; falls back to the pointer only when nothing is in focus), blink and double-blink, idle life (glance at 4 s, bored at 12 s, yawn/sigh at 20 s, doze with z's at 35 s, startle on wake) driven by an idleSince prop, hover perk-up, press = tap + happy, double-press = hop + celebrating, hold = listening (wired by the consumer), drag = carried with stretch and a settle wobble. Bigger eyes per the owner. Reduced motion: no idle life, instant expression swaps, no bounces. Accessibility: role="img" with an aria-label that names the current expression. Tests for the expression table, the idle scheduler and the target-tracking math. Regenerate the app icons and favicon from the new character (light body on white for icons; a dark variant for tinted icons) and update Logo.tsx ONLY if it embeds the character (it does not — the wordmark stays).
Gates: bun run typecheck; bunx biome check --write packages apps/web-pwa; bun run test; cd apps/web-pwa && bun run build.`, { label: 'f3:rig', phase: 'Foundations', schema: RESULT, model: 'opus', effort: 'high' }),
  () => agent(`${COMMON}\nYOU are WORKER 4 — the brain's board planner. You OWN services/gateway/src/classess_gateway/board/** (new package: planner.py, pipelines/{math.py,physics.py,chemistry.py,bio_social.py}, stream.py, verify.py), services/gateway/src/classess_gateway/wobo.py (the turn: extend, do not rewrite), services/gateway/src/classess_gateway/app.py ONLY to add the streaming route, services/gateway/src/classess_gateway/budget.py ONLY to classify the new capability, and services/gateway/tests/test_board*.py. Worker 2 generates board_schema.py from the TS grammar; until it lands, write a local copy of the JSON schema from docs/BOARD.md §2 and reconcile at the end. Build:
- The turn protocol: a streaming endpoint (SSE over the existing authenticated capability route, e.g. POST /v1/capability/wobo.turn with Accept: text/event-stream, or WS reusing the voice token pattern — choose the one that keeps auth and budget identical to the non-streaming route and document it) emitting say/ink/action/ask/card/done events with timestamps; interleaving so the first ink event precedes the end of the first sentence; interrupt handling ({interrupted_at}); resume from the last acknowledged event.
- planner.py: turns the model's structured plan (the generate/turn tier per the router; the model is asked for a compact plan in the grammar with intents like "graph y=x^2 with the tangent at x=1" rather than raw coordinates where a pipeline can compute) into validated objects; presentation choice (screen | plane | full) per BOARD.md §5 with learner override; every object validated against the schema; anchors validated against the registry snapshot in the packet (unknown target → refuse or re-anchor to board space, never float).
- pipelines: math (curves, tangents, constructions, number lines, derivations as ordered write objects with depends; numbers via sympy in the verifier's sandbox), physics (free-body, projectile decomposition, circuits, rays, waves; dimensional analysis via the existing verifier), chemistry (SMILES → atoms/bonds with 2D coordinates via RDKit in a chemist's stroke order; balancing with ticking coefficients), bio/social (labelled cells, food webs, timelines, maps from the existing scene specs and fact base). Every quantity through verify.py before emission; a failed check redraws once then refuses.
- Golden boards: a pytest suite with 12 prompts across domains asserting structure (object kinds, anchors, ordering, verified numbers), not pixels.
- Budget: board turns meter as turns; a plan over 200 objects is refused as too_much_at_once.
Gates: cd ${REPO} && uv sync --all-packages && uv run pytest -q services/gateway && uv run pytest -q services/verifier && uvx ruff check services.`, { label: 'f4:planner', phase: 'Foundations', schema: RESULT, model: 'opus', effort: 'high' }),
])
const found = f.filter(Boolean)
const APIS = found.map((r, i) => `--- worker ${i + 1} API ---\n${r.api}`).join('\n')
log(`Foundations: ${found.length}/4 returned`)

phase('Integration')
const integ = await parallel([
  () => agent(`${COMMON}\nAPIs delivered by the foundation workers:\n${APIS}\nYOU are WORKER 5 — integration into the app. You OWN apps/web-pwa/src/App.tsx, apps/web-pwa/src/wobo/** (speech.tsx, Companion.tsx, chat.tsx, voice.ts, paths/, capabilities.ts, tutor.ts), apps/web-pwa/src/shell/**, and MINIMAL hooks in apps/web-pwa/src/screens/** and src/engines/** (registering targets only). Do: mount the gesture layer and the ink renderer at the root; register every screen's targets (home thread stops, composer, chips, doors; Learn grid; Subject and chapters; course players' cards, beats, continue; Practice; Progress twin; You pickers and settings; Onboarding; Command palette; the plane itself; Download center) and every engine's semantic parts via the bus (fixing the seven unattached refs); wire the plane (summon by gesture, the word "board" in the composer or voice, palette command); on-screen ink over a paused video: MotionPlayer exposes the frame's scene objects at the paused timestamp as targets; the video handoff (annotate on frame or open the plane beside it, return to the paused position); presentation choice + learner override ("board"/"here"); the streaming turn client consuming say/ink/action/ask/card/done with speech.tsx driving the utterance clock the renderer reads; interrupt (tap/key/voice) stops pen and voice together; companion modes as composer/palette/voice entries (explain this, show me, do it, quiz me, check my work, why is this wrong, say it in my world, read it aloud, teach it back) mapping to wobo.turn intents; "show me" = a visible cursor gliding to the registry target and tapping (permission ladder for "do it"); proactive lean-in (three wrong actions or forty idle seconds → the orb leans and offers a pointer; governed by a quiet/balanced/proactive setting); the new WoboBody wired to real signals (listening from push-to-talk, drawing from the renderer, thinking while the plan streams, aha on a verified correct answer, idleSince from real idleness, eyes tracking the focus object); board timeline + save to notes + share image entry points. Keep the first-meeting intro intact.
Gates: bun run typecheck; bunx biome check --write apps/web-pwa packages; bun run test; cd apps/web-pwa && bun test test/*.test.ts src && bun run build && bunx playwright test --reporter=list.`, { label: 'i5:integration', phase: 'Integration', schema: RESULT, model: 'opus', effort: 'high' }),
  () => agent(`${COMMON}\nAPIs delivered by the foundation workers:\n${APIS}\nYOU are WORKER 6 — golden boards end to end and the bench. You OWN apps/web-pwa/tests/board*.spec.ts, apps/web-pwa/src/wobo/board-bench.tsx (extend Worker 2's bench), services/gateway/tests/test_board_golden.py (extend Worker 4's), and ${SCRATCH}/golden/**. Build 12 golden boards spanning math (Pythagoras proof, tangent to y=x^2, long division), physics (projectile at the apex, free-body on an incline, a series circuit), chemistry (benzene, balancing photosynthesis, a titration curve), biology/social (a labelled plant cell, a food web, a timeline). For each: the prompt, the expected structure (object kinds, anchors, ordering, verified numbers), a mock-mode plan fixture the bench can play without a model, and a Playwright spec that plays it in the bench and asserts the DOM (objects present, anchors resolved, the derivation written in order) plus a screenshot saved to ${SCRATCH}/golden/<name>.png in both themes. Also a latency harness: measure time-to-first-stroke from a fixture stream in the bench and assert under 1000 ms in CI-mode.
Gates: cd apps/web-pwa && bunx playwright test --reporter=list; cd ${REPO} && uv run pytest -q services/gateway/tests/test_board_golden.py.`, { label: 'i6:golden', phase: 'Integration', schema: RESULT, model: 'opus', effort: 'medium' }),
])
const integd = integ.filter(Boolean)

phase('Gate')
const gate = await agent(`${COMMON}\nAll workers reported: ${JSON.stringify([...found, ...integd].map(r => ({ summary: r.summary, gates: r.gates, open_issues: r.open_issues })))}. YOU are the GATE agent; every file is yours now. Run from ${REPO}: bun install --frozen-lockfile || bun install; bun run typecheck; bunx biome check --write . && bunx biome check .; bun run test; cd apps/web-pwa && bun test test/*.test.ts src && bun run build && bunx playwright test --reporter=list; cd ${REPO} && uv sync --all-packages && uv run pytest -q services/gateway && uv run pytest -q services/verifier && uvx ruff check services content. Fix every failure at the root; never weaken a test. Then LIVE PROOFS with the web app in mock mode on port 5183 and the gateway in mock mode on 8081 (DEV_AUTH=1, ENV=dev): using Playwright, (a) open a lesson with a video, pause it, lasso the frame, ask "why?", and screenshot Wobo's ink on the frame; (b) ask a derivation question and screenshot the plane mid-write and complete; (c) run "show me how to change my board" and screenshot the cursor at the control; (d) the dev inspector overlay showing registered targets on Home; (e) light and dark of (b); (f) the character in listening, drawing, celebrating; save to ${SCRATCH}/proof/*.png; measure time-to-first-stroke; kill both servers. Return gates and the proof list.`, { label: 'gate:full+proofs', phase: 'Gate', schema: RESULT, model: 'opus', effort: 'high' })

phase('QA')
const qa = await agent(`${COMMON}\nYOU are the QA adversary (read-only on the repo; write under ${SCRATCH}/qa). The gate reported: ${JSON.stringify({ summary: gate?.summary, gates: gate?.gates, notes: gate?.notes })}. Break the board: pixel anchoring anywhere (grep for absolute coordinates outside board space), marks that float after scroll/resize/theme change, numbers that come from the model instead of code (trace planner outputs), ink landing after the word it refers to, plans that fail validation but still render, a plane that hides what it explains, keyboard dead ends (every gesture and mode via keyboard only), screen-reader silence on focus objects, reduced-motion violations (animations running with prefers-reduced-motion), performance (2,000 strokes at 60 fps on a throttled CPU via Playwright's CDP emulation), memory leaks (mount/unmount the plane 50 times), the character's idle life firing during speech, provider names anywhere in new copy, and any copy with exclamation marks or emoji. Reproduce each with a script or a test; list file:line, severity, minimal fix.`, { label: 'qa:adversary', phase: 'QA', schema: RESULT, model: 'opus', effort: 'high' })

phase('Fix')
const fix = await agent(`${COMMON}\nYOU are the FIXER; every file is yours. QA reported: ${JSON.stringify({ notes: qa?.notes, open_issues: qa?.open_issues })}. Fix every real finding at the root with a regression test, re-run the full gates (typecheck, biome, bun tests, web-pwa tests, build, Playwright, gateway + verifier pytest, ruff) and re-run the live proofs from the gate, updating ${SCRATCH}/proof/. Report anything deliberately left and why.`, { label: 'fix:close', phase: 'Fix', schema: RESULT, model: 'opus', effort: 'high' })

return { foundations: found.map(r => ({ summary: r.summary, gates: r.gates, open_issues: r.open_issues, api: r.api })), integration: integd.map(r => ({ summary: r.summary, gates: r.gates, open_issues: r.open_issues })), gate: gate ? { summary: gate.summary, gates: gate.gates, notes: gate.notes, open_issues: gate.open_issues } : null, qa: qa ? { notes: qa.notes, open_issues: qa.open_issues } : null, fix: fix ? { summary: fix.summary, gates: fix.gates, notes: fix.notes, open_issues: fix.open_issues } : null }
