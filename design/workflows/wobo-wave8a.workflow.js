export const meta = {
  name: 'wobo-wave8a-reskin',
  description: 'Wave 8a: the re-skin — a component kit from DESIGN.md v4, every app screen ported to the five Fable reference screens (app-v1.html), the public site shell and Wave 7b pages ported to the six Fable site prototypes, the six new pitch pages (Security, Meet Wobo, For parents, For students, How it works, Subjects) built verbatim; gated by typecheck, biome, tests, build, the responsive harness at three widths in both themes, the brand gates, and an adversarial visual QA against the prototype frames',
  phases: [
    { title: 'Kit', detail: 'one worker: tokens + primitives from DESIGN.md and app-v1.html' },
    { title: 'Build', detail: 'app screens (three workers), site shell + 7b pages, then the six pitch pages' },
    { title: 'Gate', detail: 'gates + responsive harness + brand gates + visual QA' },
    { title: 'Fix', detail: 'close findings, re-gate' },
  ],
}
const REPO = args.repo, SCRATCH = args.scratch, PROTO = args.proto
const COMMON = `Repo: ${REPO} (branch the-life; Bun workspaces @wobo/*; Vite/React PWA at apps/web-pwa; Python wobo_gateway at services/gateway). LAW: read DESIGN.md end to end first (bold ink on good paper, palette v4, no border lines, tonal surfaces, radius 10/16/24, ink strokes 3-4px, soft tinted shadows only under floating things, Poppins + Caveat only, spacing scale --s1..--s6, no dust, ribbon cursor on marketing only). THE REFERENCE SCREENS ARE FABLE'S HAND: ${PROTO}/app-v1.html (Home, Learn, Lesson, Practice, You; desktop rail + phone bottom-tab layout; frames app-{1..5}-{light,dark,m}.png beside it), ${PROTO}/states-v2.html (loading, 404, 500, offline, limit, expired, maintenance), ${PROTO}/onboarding-v2.html, and the eight site pages ${PROTO}/site-{security,meet,parents,students,how,subjects,plans,about}.html with frames {sec,meet,par,stu,how,sub}-{L,D,M}-NN.png. Port structure, spacing, tokens and COPY VERBATIM from the prototype; never invent copy, never add a feature, never explain the mechanism ('every board on demand' stays secret). Wobo has no gender (§19): name first, they/them only when unavoidable; the pronoun CI gate greps for it. Neutral by default (§20). White-label: no vendor or model names anywhere in UI copy. DO NOT TOUCH apps/web-pwa/src/screens/landing/** (Wave 7c owns it and may still be running), packages/wobo/src/board/**, services/gateway/** or App.tsx beyond adding lazy routes. Do not run git commit; leave the tree for Fable's gate. When you finish, return the structured result.`
const RESULT = { type: 'object', required: ['summary', 'changed_files', 'gates', 'open_issues'], properties: { summary: { type: 'string' }, changed_files: { type: 'array', items: { type: 'string' } }, gates: { type: 'object', properties: { typecheck: { type: 'string' }, biome: { type: 'string' }, tests: { type: 'string' }, build: { type: 'string' } } }, open_issues: { type: 'array', items: { type: 'string' } } } }
const FINDINGS = { type: 'object', required: ['summary', 'findings', 'gates'], properties: { summary: { type: 'string' }, gates: { type: 'object' }, findings: { type: 'array', items: { type: 'object', required: ['screen', 'severity', 'what', 'prototype_frame', 'app_frame'], properties: { screen: { type: 'string' }, severity: { type: 'string', enum: ['blocker', 'major', 'minor'] }, what: { type: 'string' }, prototype_frame: { type: 'string' }, app_frame: { type: 'string' }, fix_hint: { type: 'string' } } } } } }

phase('Kit')
const kit = await agent(`${COMMON}\nYOU are WORKER A — THE KIT. You OWN apps/web-pwa/src/ui/** (create it) and the app's global tokens/stylesheet (find the existing one: grep apps/web-pwa/src for '--ink' or the old palette hexes; replace it with palette v4 tokens for light and dark, both stamped [data-theme] and prefers-color-scheme, exactly the :root blocks at the top of ${PROTO}/app-v1.html). Build primitives as React components with plain CSS modules or a single ui.css, no new dependencies: Button (ink / pig / quiet / sm), Chip (+on), Label (uppercase pig), Tag (uppercase ink-3), Card (tonal tints pig/mint/rose/marigold/lilac/paper-2), Tile, Pill, Toggle, Segmented, AskBox (input + mic + Ask), Sticker (marigold, rotated), HandNote (Caveat block with rose emphasis), StreakDays, AllowanceCard, AppShell (desktop rail with wordmark + four nav items + bottom slot; phone bottom tab bar at <=900px per app-v1.html's mobile block), TopBar (crumb + right slot), WoboHead (wrap the EXISTING character rig in packages/wobo — do not redraw it; head only, no body). Every primitive must match app-v1.html pixel-for-pixel in both themes. Write a Storybook-free gallery route /ui-kit (dev only, behind import.meta.env.DEV) that renders every primitive in both themes so the gate can screenshot it. Remove old primitives only where nothing else imports them; otherwise leave and note. Gates: bun run typecheck, bunx biome check apps/web-pwa, bun test, bun run build (from ${REPO}).`, { label: 'A:kit', phase: 'Kit', schema: RESULT })

phase('Build')
const built = (await parallel([
  () => agent(`${COMMON}\nThe kit is in place: ${JSON.stringify(kit ? kit.summary : 'kit worker returned nothing — build against app-v1.html directly and create what you need under apps/web-pwa/src/ui/')}.\nYOU are WORKER B — HOME and LEARN. You OWN the Home screen and the Learn screen (find them: grep apps/web-pwa/src/screens for the route components mounted at / (authenticated), /home, /learn, /courses) and the authenticated app shell mount. Port boards 01 and 02 of ${PROTO}/app-v1.html exactly: Home = TopBar crumb (weekday · class · board) + streak chip + avatar; greeting 'Hey <name>,' in Caveat pig then 'what are we figuring out tonight?'; the one-paragraph situational sub (built from real data: next test, lessons in, minutes to ready — if the data does not exist, render the copy from the real fields you have and leave a TODO comment naming the missing field); AskBox; three today cards (Continue / Practice / Wobo noticed) from real plan data; 'This week, in Wobo's words' HandNote from the existing weekly summary; streak card with 'Rest days don't break it. Learning does not need guilt.' Learn = 'Your subjects' label, headline 'Mathematics, where your class is this week', four subject tiles (outline on the active one), unit rows (done / now / next / later states, progress bar in mint, the official-syllabus provenance pill), and the Wobo line 'Something your school does differently? Tell me and I'll reorder, add or drop a chapter for you.' Wire everything to the existing data hooks; no mock data in production paths. Phone layout per app-v1.html's mobile block. Gates as above.`, { label: 'B:home+learn', phase: 'Build', schema: RESULT }),
  () => agent(`${COMMON}\nThe kit is in place: ${JSON.stringify(kit ? kit.summary : 'kit worker returned nothing — build against app-v1.html directly')}.\nYOU are WORKER C — LESSON and PRACTICE. You OWN the lesson/board screen chrome and the practice screen (find them: grep apps/web-pwa/src/screens for the lesson, board, plane, practice route components) and the answer-kind skins in packages/wobo/src/answers/** (skin only; keep their logic and tests). Port boards 03 and 04 of ${PROTO}/app-v1.html exactly: Lesson = TopBar crumb + segmented Full board / Plane / Notes; the plane card (bar: 'Wobo · with <name>' + live 'drawing' pill; the canvas is the EXISTING board renderer — do not touch its internals, only the frame around it: paper canvas inset in a paper-2 card, radius 18/26); the say row (mini WoboHead + Caveat line + one action button); the side column (This lesson steps with the current one in pig; 'Ask about this' rose card with Circle / Type / Talk chips; 'Your place' card); the rail's bottom slot becomes the hold-to-talk pill ('space · Hold to talk to Wobo'). Practice = mint item card (question with Caveat fraction, the answer kind, Check / Start over, Wobo's Caveat line 'one more, and you're there' / the ring-the-gap annotation, small WoboHead bottom-right), side column with the set list (current row pig-w) and the marigold 'How this works' card with the exact copy. Phone layout per the mobile block (side column stacks under, hold-to-talk hidden in the bar). Gates as above.`, { label: 'C:lesson+practice', phase: 'Build', schema: RESULT }),
  () => agent(`${COMMON}\nThe kit is in place: ${JSON.stringify(kit ? kit.summary : 'kit worker returned nothing — build against app-v1.html directly')}.\nYOU are WORKER D — YOU, SETTINGS, PARENT, ONBOARDING, STATES. You OWN the You/progress screen, Settings, the parent screens (companion/parent view), onboarding, and the state screens (loading, 404, 500, offline, limit reached, session expired, maintenance). Port board 05 of ${PROTO}/app-v1.html exactly (This week in Wobo's words + bar chart; Learning strengths with the three drawn icons; Parents card 'Share the week with a parent' with 'Send an invite' + 'not linked yet' pill; Settings list: Wobo speaks replies out loud (voice chosen for <country> · English), Reduce motion, Appearance Light/Dark/Auto, 'Festivals we can wish you on' (chosen by your family · none yet), 'Your data' (Export or delete everything, any time → the existing erase flow which now calls POST /v1/me/erase); rail bottom slot = 'Your plan · Free · 40 turns a day · See Pro'). Onboarding from ${PROTO}/onboarding-v2.html; states from ${PROTO}/states-v2.html (the ink scenes, 3.5px strokes, real wordmark, Wobo present, never a dead end). Parent view: the mock in ${PROTO}/site-parents.html chapter 02 is the visual reference (lessons list, the Sunday note in marigold-w, the lock line 'Questions word for word: shared only if <name> allows'). Phone layout per the mobile block. Gates as above.`, { label: 'D:you+parent+states', phase: 'Build', schema: RESULT }),
  () => agent(`${COMMON}\nThe kit is in place: ${JSON.stringify(kit ? kit.summary : 'kit worker returned nothing — build against the prototypes directly')}.\nYOU are WORKER E — THE SITE SHELL and the Wave 7b PAGES. You OWN apps/web-pwa/src/screens/site/** (SiteShell), screens/legal/**, screens/auth/**, and the about, help, plans, gift, contact, sitemap pages. Rebuild SiteShell to the shell every site prototype shares (open any ${PROTO}/site-*.html): sticky blurred header with the real wordmark symbol, the pill nav (Meet Wobo · How it works · For parents · For students · Subjects · Plans, active item ink), Sign in (quiet) + Get started (pig); footer (Wobo / For / Help / Company columns, exact links incl. 'Security and trust'); the ink close panel ('Begin tonight.' + Caveat marigold 'The first question is on us.' + marigold button + quiet button) as a reusable ClosePanel with props; the AskWobo block (WoboHead + label + h2 + AskBox + chips) as a reusable component; the reveal-on-scroll helper (visible at rest, prefers-reduced-motion respected). Then re-skin every 7b page to this language: About (port ${PROTO}/site-about.html verbatim), Help centre (+ articles), Plans (port ${PROTO}/site-plans.html verbatim: the India/everywhere toggle, the three plan cards, the honest table, the checkout preview with the two consent boxes, the gift block, the money FAQ; prices come from the plan tiers in the brain — Pro ₹1,999 / $20, Max ₹3,999 / $50 — never hardcoded on the homepage), Gift, Contact, Sign in / Sign up, Legal (the 'in plain words' card), Sitemap. Keep their existing copy from docs/copy/** and docs/legal/**. Gates as above plus the brand gates (bun run gate).`, { label: 'E:site-shell+7b', phase: 'Build', schema: RESULT })
    .then(e => agent(`${COMMON}\nSite shell worker reported: ${JSON.stringify(e ? { summary: e.summary, changed_files: e.changed_files, open_issues: e.open_issues } : 'nothing — build the shell yourself under screens/site/ before the pages')}.\nYOU are WORKER F — THE SIX PITCH PAGES. You OWN new routes and files under apps/web-pwa/src/screens/pitch/**: /security (${PROTO}/site-security.html), /meet-wobo (${PROTO}/site-meet.html), /for-parents (${PROTO}/site-parents.html), /for-students (${PROTO}/site-students.html), /how-it-works (${PROTO}/site-how.html), /subjects (${PROTO}/site-subjects.html). Port each page VERBATIM — every section, every word of copy, every drawn SVG (paste the SVG markup, keep the .draw stroke reveal and the .reveal scroll helper), every interactive moment as real React state: Meet Wobo's eyes-follow-pointer and typed first line and the hold-space 'Hey Wobo' mock; For students' colour-half puzzle with the ring and the turn-the-ray angle; How it works' drag-the-point on y = 2x + 1; Security's request-the-overview form (POST to the existing contact endpoint if there is one, else a mailto fallback and a TODO). Use the shell, ClosePanel and AskWobo from worker E; wire the AskWobo inputs to the existing help-grounded Ask Wobo capability where it exists (unauthenticated, tiny budget) else leave the prototype's static behaviour with a TODO. Add the routes to the router as lazy chunks and to the sitemap. Both themes; three widths. Gates as above plus bun run gate.`, { label: 'F:pitch-pages', phase: 'Build', schema: RESULT })),
])).filter(Boolean)

phase('Gate')
const gate = await agent(`${COMMON}\nWorkers reported: ${JSON.stringify(built.map(r => ({ summary: r.summary, gates: r.gates, open_issues: r.open_issues })))}; kit: ${JSON.stringify(kit ? { summary: kit.summary, gates: kit.gates } : null)}.\nYOU are the GATE + VISUAL QA ADVERSARY for Wave 8a. From ${REPO} run: bun run typecheck; bunx biome check .; bun test; bun run build; bun run gate (brand gates); then the responsive harness (tests/responsive.spec.ts — read it; run it at 390, 834 and 1440 in light and dark) and save every frame under ${SCRATCH}/frames/. Then compare, screen by screen, the app frames against ${PROTO}/app-{1..5}-{light,dark,m}.png and the site frames against ${PROTO}/{sec,meet,par,stu,how,sub}-{L,D,M}-01..NN.png and ${PROTO}/states-*.png. Your job is to REFUTE the claim 'this matches Fable's prototype': look for border lines that should not exist, strokes thinner than 2.5px, wrong radii, wrong tints, shadows under things that do not float, Inter or system fonts leaking, copy that differs by a word, missing sections, missing interactive moments, phone layouts that keep the desktop rail, dark theme colours that are inverted rather than designed, any vendor or model name, any gendered pronoun for Wobo. Every finding names the screen, the severity, the prototype frame and the app frame. Return findings sorted blocker → minor plus the gate outputs.`, { label: 'gate:8a', phase: 'Gate', schema: FINDINGS, effort: 'high' })

phase('Fix')
const fix = gate && gate.findings && gate.findings.length
  ? await agent(`${COMMON}\nYOU are the FIXER for Wave 8a; you own exactly the files the workers created or changed (${JSON.stringify([...(kit ? kit.changed_files : []), ...built.flatMap(r => r.changed_files)].slice(0, 300))}). The gate found: ${JSON.stringify(gate.findings)}. Close every blocker and major, and every minor you can, without inventing copy or features; re-run bun run typecheck, bunx biome check ., bun test, bun run build, bun run gate and the responsive harness; re-shoot the frames the findings named into ${SCRATCH}/frames-fixed/ and confirm each one now matches. Return what you fixed, what you could not, and the final gate outputs.`, { label: 'fix:8a', phase: 'Fix', schema: RESULT })
  : null

return { kit: kit ? { summary: kit.summary, gates: kit.gates, open_issues: kit.open_issues } : null, built: built.map(r => ({ summary: r.summary, gates: r.gates, open_issues: r.open_issues })), gate: gate ? { summary: gate.summary, gates: gate.gates, findings: gate.findings } : null, fix: fix ? { summary: fix.summary, gates: fix.gates, open_issues: fix.open_issues } : null }
