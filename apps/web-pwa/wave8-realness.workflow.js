export const meta = {
  name: 'wave8-realness',
  description: 'Owner prod-walkthrough charter: Vidya first-principles review then fix, voice UX, course correctness, visual-first content, conversational onboarding, UI sweep, verify',
  phases: [
    { title: 'Rethink', detail: 'first-principles Vidya architecture verdict', model: 'opus' },
    { title: 'Fix', detail: 'six parallel missions, all Opus', model: 'opus' },
    { title: 'Verify', detail: 'gates + e2e on every charter item', model: 'opus' },
  ],
}

const ROOT = '/Users/depl/Documents/classess-learner'
const BRIEF = `You work on Classess Learner at ${ROOT} (branch the-life). LAW FILES first: CONTEXT.md, DESIGN.md (white canvas, 3px radii, vibrant accents, motion first-class, end-to-end layouts), VIDYA.md, SUBJECTS.md. Fleet brief: /private/tmp/claude-501/-Users-depl-Documents-classess-learner/6aa90296-d29d-4047-861b-907e346f7b79/scratchpad/fleet-brief.md (read the STATE UPDATE section). The app runs at http://localhost:5174 (vite dev, live gateway on :8787); production is Vercel + Railway. Gates for every mission: bun run typecheck (apps/web-pwa) clean, biome check --write on touched files, and verify your behavior live with the playwright MCP. Do NOT commit. OWNER FEEDBACK VERBATIM you are fixing (from the live prod walkthrough): "the mic doesnt work properly, the slider overlaps with the top right functions like the profile and stuff; Vidya honestly doesnt annotate the way we want to and it does the same annotations for everything, the hint is weird repeating itself and very small, vidya latency is very delayed, it transcribes first and then talks, the questions arent read out as we keep moving, the user should have a mute button on the screen; the content feels text heavy, its not creative and visual first, the way we teach isnt formulated properly; create a custom course with Vidya doesnt work; i dont want the users avatar to be letters, allot a random character by default; courses always start from first, progress tracking is important; the next button shouldnt unlock until vidya finishes reading or the same seconds pass; replay button next to the mute button; MCQs should highlight the correct answer when checked wrong; chapter progress depicted in a cooler way; onboarding shouldnt feel like a form, it should feel like talking to AI, gather info subtly (likes for examples, AGE mandatory)."\n\nYOUR MISSION:\n`
const R = { type: 'object', properties: { built: { type: 'string' }, files: { type: 'array', items: { type: 'string' } }, verified: { type: 'string' }, unfinished: { type: 'string' } }, required: ['built', 'files', 'verified', 'unfinished'] }

// ---- Phase 1: think before touching anything -------------------------------------------------
phase('Rethink')
const verdict = await agent(BRIEF + `FIRST-PRINCIPLES VIDYA ARCHITECTURE REVIEW — the owner's words: "i feel like with vidya you are overwriting code instead of thinking about it from a fresh angle and seeing if the code is even right to achieve the capabilities we need." Do NOT write any product code. Read the ENTIRE Vidya stack end to end: packages/vidya/src/* (context-bus, actions, highlight-overlay, body), apps/web-pwa/src/vidya/* (Companion, Flight, chat, speech, voice, paths/, capabilities, tutor), services/gateway/src/classess_gateway/vidya.py + voice.py, and every useRegisterTarget call site across screens. Then answer with brutal honesty, capability by capability: (1) ANNOTATION VARIETY — why does she draw the same marks everywhere? Trace the real cause (registry coverage? prompt? action schema too narrow? overlay rendering only one mark type well?). (2) GROUNDED PERCEPTION per page — which screens expose real state and targets, which are blind spots. (3) LATENCY — the full turn path with real timings (measure with curl against localhost:8787), where the seconds actually go, and the cheapest reordering to make her FEEL instant (speech-first, sentence-chunk TTS, optimistic ink). (4) HINT PIPELINE — why hints repeat and read tiny. (5) MIC — trace the relay flow in prod (wss through Railway single instance; token gate; sample rates) and name the break. (6) Is the five-paths/capabilities/tutor architecture from wave-4 the RIGHT shape for "a brain that thinks then acts", or does anything need restructuring? For each: verdict, root cause, and the SMALLEST correct redesign. Write the full report to /private/tmp/claude-501/-Users-depl-Documents-classess-learner/6aa90296-d29d-4047-861b-907e346f7b79/scratchpad/wave8-vidya-verdict.md AND return a condensed version (root causes + prescriptions per capability).`, { label: 'vidya-verdict', phase: 'Rethink', model: 'opus', effort: 'xhigh', schema: { type: 'object', properties: { report: { type: 'string' }, prescriptions: { type: 'array', items: { type: 'string' } } }, required: ['report', 'prescriptions'] } })

log('verdict in — dispatching fixes')

// ---- Phase 2: six missions, each grounded in the verdict --------------------------------------
const V = () => `\n\nTHE ARCHITECTURE VERDICT (follow its prescriptions; full report at scratchpad/wave8-vidya-verdict.md):\n${JSON.stringify(verdict.prescriptions)}\n`
const fixes = await parallel([
  () => agent(BRIEF + `VIDYA ACTS RIGHT. Implement the verdict's prescriptions for annotation variety + grounded perception + hints: every screen worth acting on registers rich targets (subject rows, course cards, practice items, progress twin); the gateway prompt exposes the FULL mark vocabulary (underline/circle/arrow/bracket/check/crossOut/lookHere + draw/write/point/highlight levels) with guidance to pick the mark that fits the pedagogical moment, never a default; overlay renders every mark type beautifully (hand-drawn, distinct); hints escalate through the assistance ladder without repeating (dedupe against recent hints in context) and render at a readable size in a proper hint surface. Verify live: three different asks on three different screens must produce visibly different, correctly-anchored ink.` + V(), { label: 'acts-right', phase: 'Fix', model: 'opus', effort: 'high', schema: R }),
  () => agent(BRIEF + `VOICE & NARRATION UX. (1) Mic: fix per the verdict (relay flow, token, prod wss). (2) Latency: make her FEEL instant — speech starts with the first sentence (chunk TTS by sentence: synthesize sentence 1 immediately while the rest queues; reuse /v1/voice/tts), ink dispatches optimistically, no transcribe-then-talk feel. (3) COURSE NARRATION: every course card's core text is read aloud on arrival (same voice, respects mute); QUESTIONS read out as the learner moves. (4) On-screen MUTE + REPLAY buttons on the course stage (reuse MuteButton from src/vidya/speech.tsx; replay re-speaks the current card). (5) NEXT-GATING: the advance button stays locked until narration completes — or, when muted, until an equivalent reading time passes (estimate from text length); show a subtle fill-up affordance on the locked button, never a dead button. Verify live on the atom course.` + V(), { label: 'voice-ux', phase: 'Fix', model: 'opus', effort: 'high', schema: R }),
  () => agent(BRIEF + `COURSE PLAYER CORRECTNESS. (1) RESUME: courses must NEVER restart — generalize the clss-course-pos-v1 position store (see AtomJourney's go()) to EVERY player including the generated-course player (Composing/theatre path); restore on mount with a quiet "picking up where you left off" beat; verify by entering, advancing 3 cards, leaving, re-entering. (2) MCQ: checking a wrong answer highlights the correct one (green, after the shake) alongside the learner's wrong pick (retry hue) — with a one-line why. (3) CHAPTER PROGRESS, cooler: design + implement a more alive depiction of per-chapter progress on the subject page and course header (current: plain fill bars; think ignition arcs on the sigils, a filament that lights stop by stop — DESIGN.md law, small and premium, no gimmick). Verify each behavior live.`, { label: 'course-correct', phase: 'Fix', model: 'opus', effort: 'high', schema: R }),
  () => agent(BRIEF + `VISUAL-FIRST CONTENT + VIDEOS + CUSTOM COURSES. (1) Restructure lesson composition to guided-discovery law: one idea per screen, interaction BEFORE prose, text per card hard-capped (~60 words), every numerical gets its visual (SimRunner/DiagramView/GeneratedImage already exist as runners — USE them in composed cards). Fix the compose prompt/pipeline in services/gateway plexus engine.compose so generated courses come out visual-first and properly SEQUENCED pedagogically (hook → explore → formalize → practice → boss). (2) VIDEOS: wire engine.video (motion scenes + TTS narration, MotionPlayer exists) into composed courses — and when a topic naturally teaches in bits, compose MULTIPLE SHORTER videos (one per sub-idea, each as long as it needs: 10s-120s) placed at the right beats of the course rather than one monolith; at least one video per topic either way, generated on first open with an honest placeholder + ready state. Owner's bar: "high quality content, visual heavy" — the video's motion scenes must genuinely animate the idea (moving parts, annotated moments), never slideware. VIDEO MODEL ROUTING LAW (owner 2026-07-07): the orchestration/scene-plan prompts are law (do not weaken them); the gateway video engine generates scenes on frontier.sonnet by DEFAULT and escalates to frontier.reason (Opus) ONLY when necessary — a complexity flag in the scene plan or a failed structural/quality validation triggers the escalation; wire this into the engine.video routing (registry/routing.py) with a test. (2b) WORKBOOKS (owner law): the MINI workbooks inside lessons and the FULL workbooks in Practice/boss must be HIGHLY interactive and visual — a mix of match-the-following, drag-and-drop, fill-in-the-blanks, tap-to-reveal, label-the-diagram (live SVG), ordering puzzles — never a static list of questions; each interaction gives immediate felt feedback (springs, ink, the sound layer when it lands). (3) CREATE A CUSTOM COURSE WITH VIDYA: find why it doesn't work (trace from the chat/create path end to end) and fix — a learner asking Vidya to learn something out-of-syllabus must get a real composed course for their age. Verify by generating one custom course live.`, { label: 'content-visual', phase: 'Fix', model: 'opus', effort: 'high', schema: R }),
  () => agent(BRIEF + `ONBOARDING AS A CONVERSATION. Rebuild apps/web-pwa/src/screens/Onboarding.tsx so it feels like meeting Vidya, not filling a form: she talks (her voice + typed lines), the learner answers in a chat-like flow with quick chips where natural; she gathers name, AGE (mandatory — drives age-branch), board+grade, and their LIKES/interests (sports, games, music — subtly: "what do you do when you're not studying?"), without it feeling like data collection. Persist to the existing learner profile + mind.ts interests slot so analogies/examples can use them. Keep the existing auth beat (phone OTP/Google when live) woven into the conversation, not bolted on. It must stay fast (a keen learner gets through in under a minute), fully responsive, and end with the flying-into-home moment. Verify the full flow live including that age+likes persist.`, { label: 'onboarding-convo', phase: 'Fix', model: 'opus', effort: 'high', schema: R }),
  () => agent(BRIEF + `ADVENTURE ROADMAP + CONSTELLATION + COURSE INTROS. (1) ADVENTURE ROADMAP: a second way to see a subject's chapters — one continuous journey where EACH CHAPTER IS A BIOME with its own vibe (forest → snow → lava → ocean → night sky → …, get creative, deterministic per chapter via the existing hash/rng art helpers in src/ui/art.tsx), the learner's trail winding through them stop by stop, current position marked with life, all pure SVG in DESIGN.md law (white canvas, vibrant accents, premium never cartoon-cheap). Keep the EXISTING chapter list — add a view preference in You settings (clss-view-pref-v1: 'list'|'adventure') that SubjectScreen honors, with a small in-page toggle too. (2) CONSTELLATION: the progress twin's constellation is too faint — remove its washed background panel and draw the constellations directly on the page, stars and links clearly visible (ink-strength lines, earned stars in subject hues), still light-theme. (3) COURSE INTROS: every course arrival card currently shows the same basic graphic — make the intro art PER-COURSE: derive a unique scene from the topic's sigil geometry + subject hue + cast/props (art helpers exist), so two courses never open identically. Verify all three live with screenshots.`, { label: 'adventure-view', phase: 'Fix', model: 'opus', effort: 'high', schema: R }),
  () => agent(BRIEF + `LEVELS + XP STARS. XP exists but LEVELS do not. (1) Build the level system in src/store/progress.tsx: a smooth curve (e.g. level n needs ~n*120 xp cumulative — tune so early levels come fast for hook, later ones stretch; pure function xp→{level, intoLevel, toNext}), persisted implicitly via xp. (2) LEVEL BADGE: show the learner's level as a small premium badge joined to the avatar in the AppHeader (and bigger in You), with the XP-to-next-level visible (ring or fraction) — the header must never get crowded; design it into the existing cluster. (3) XP EFFECTS AS STARS: when xp is earned, the bloom becomes a small star-burst (2-4 tiny stars arcing out and fading, subject-hued, spring physics) rising from the header cluster — replace the plain "+n xp" float with stars + the number; level-up gets a bigger one-time moment (badge pulses, star ring). Respect reduced-motion. Verify live: earn xp in a course, watch stars + counter + toNext update; cross a level boundary and see the level-up beat.`, { label: 'levels-stars', phase: 'Fix', model: 'opus', effort: 'high', schema: R }),
  () => agent(BRIEF + `PROGRESS REPORT + TROPHY ROOM + SOUND DESIGN. (1) PROGRESS REPORT: give the learner a real report of themselves — strengths and areas-to-improve derived from actual evidence (mastery bands, per-topic accuracy/latency from the mind + progress stores), progress graphs over time (activity counts exist in clss-activity-counts-v1), and a TRAJECTORY projection: "at this pace you reach X by <date>" (simple linear pace over the recent window — honest, not astrology; the ProgressScreen daylight twin is the home for this, extend it, don't bolt on a new page). (2) TROPHY ROOM: grow the You trophy shelf into a proper room — every earned thing (boss wins, streaks, mastered topics, teach-backs) as a beautiful object on display (generative sigils/BossSigil exist in src/ui/art.tsx), with empty pedestals hinting at what's earnable next. (3) SOUND DESIGN: NEW src/ui/sound.ts — a tiny synthesized WebAudio sound layer (no assets): soft tick on advance, a small bloom on correct, a warm chord on XP/award, a whoosh on page transitions, a gentle chime when Vidya lands — subtle, premium, never noisy; ONE global sound toggle that follows the existing mute preference (clss-voice-muted-v1 silences everything); wire at the kit level (MagneticButton taps, Screen transitions, award() in progress store) so it's everywhere without per-screen wiring. Verify live: sounds fire on the right beats and mute kills them all.` + V(), { label: 'report-trophies-sound', phase: 'Fix', model: 'opus', effort: 'high', schema: R }),
  () => agent(BRIEF + `DARK MODE — subtle, never heavy (owner: "it should be subtle, i leave you to it"). The palette is DECIDED (Fable's spec, follow it exactly): canvas #17181C (soft graphite — never #000), tonal surface #1F2026, hairline/border #2A2B32, ink #F2F2F5 primary / #B4B6BF secondary / #7A7D88 tertiary, accents unchanged EXCEPT ultramarine lightens to #4D63F2 on dark (contrast), feedback hues brightened just enough to pass contrast, frost/glass rgba(23,24,28,0.62), depth via borders and subtle glows instead of heavy shadows; Vidya's molten body needs no change (she glows beautifully on graphite) but her light-beam/shadow needs a dark variant. MECHANISM: data-theme="light|dark" on the document root + CSS custom properties — extend the existing --clss-* token layer (packages/config css + apps/web-pwa/src/main.tsx injection) with a dark block; then SWEEP the app: convert hardcoded hexes in src/ui/kit.tsx, AppHeader, screens, Companion, ChatScreen to var() references (this is the bulk of the work — be systematic, file by file; anything intentionally fixed like Vidya's molten palette stays literal). SETTINGS: appearance picker in You — light / dark / system (clss-theme-v1; 'system' follows prefers-color-scheme live via matchMedia listener). Default: system. REFERENCE (owner shared Brilliant's dark home, 2026-07-07 — inspiration NEVER replication): what works there = cards delineated by hairline borders on a dark field (no shadows), vibrant accents popping hard (their blue CTA), gradients reserved for the premium moment only; what we do DIFFERENTLY = our canvas stays graphite #17181C, softer than their near-black — the owner wants subtle, not heavy. Verify live in both themes at 3 widths, screenshot pairs of home/subject/course/chat/you.`, { label: 'dark-mode', phase: 'Fix', model: 'opus', effort: 'high', schema: R }),
  () => agent(BRIEF + `UI SWEEP. (1) The Companion drawer must never overlap the top-right header cluster (profile/xp/streak) — fix layering/geometry so both live comfortably (drawer starts below the header, or header stays above and clickable; check z-index vars). (2) DEFAULT AVATAR: never letters — on first boot allot a random cast character (persist clss-avatar-v1 {kind:'cast'}; keep upload/initial as choices in the picker only). (3) Then a full refinement pass: every screen at 1440/900/390 widths — spacing, alignment, hover states, focus rings, dead zones, anything that reads vibe-coded; fix what you find, list what you fixed. Respect DESIGN.md and the second-cut system in src/ui/kit.tsx. Verify with playwright screenshots per screen per width.`, { label: 'ui-sweep', phase: 'Fix', model: 'opus', effort: 'high', schema: R }),
])

// ---- Phase 3: prove it ------------------------------------------------------------------------
phase('Verify')
const done = fixes.filter(Boolean)
const harden = await agent(BRIEF + `VERIFY THE CHARTER. Mission reports: ${JSON.stringify(done.map((f) => f.built.slice(0, 300)))}. Run every gate: apps/web-pwa typecheck + biome + bun test, gateway uv run pytest, vite build. Then Playwright e2e against localhost:5174, one check per charter item: drawer/header no overlap; random-character default avatar on fresh profile; course resume after leaving mid-course; MCQ wrong-check shows correct answer; narration plays + next locked until it ends + replay/mute on stage; three distinct annotation asks → distinct ink; custom course creation completes; onboarding conversation captures age+likes. Fix small breaks yourself; report anything structural. Return per-item PASS/FAIL with evidence.`, { label: 'verify8', phase: 'Verify', model: 'opus', effort: 'high', schema: R })

return { verdictPrescriptions: verdict.prescriptions, fixes: done.map((f) => ({ built: f.built.slice(0, 400), verified: f.verified.slice(0, 200) })), harden }
