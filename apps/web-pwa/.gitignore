test-results/
playwright-report/
blob-report/
.playwright/
.vercel

# Secrets: every env file is ignored EXCEPT the committed, non-secret production
# flag file and the template. Negations must stay last (last match wins).
.env
.env.*
!.env.example
!.env.production
