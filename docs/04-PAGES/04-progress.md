# 04 · Progress (the map of a mind)

**Purpose:** make the learner's cognition visible and queryable (see `03-FLOWS/07-progress-knowledge-twin-flow.md`).
**Contains:** the **ConstellationMap** (prereq graph; mastered nodes glow their color, rest monochrome; edges show unlocks) · **twin query**
(strengths/weaknesses/what-next, honest ranges) · next-best action · goal-gradient cues near completion.
**States:** map(dark/partly-lit/rich) · twin-asking · twin-answer(ranges) · goal-gradient · empty (pre-diagnostic — but diagnostic pre-lights it).
**Wobo:** can "tour" the map with the learner per the license (drifting to nodes Wobo references), flame calm and proud.
**Events:** `twin.query.asked.v1`; reads `mastery.band.changed.v1`.
**AI:** `twin.query` · `mastery.getNextBestNode`.
**Guardrails:** honest ranges not guarantees; the learner's own data shown back (works fully un-elevated); no behavioural shaping; no feed.
