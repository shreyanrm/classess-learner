# Phase 2 · The Loops (generalize the mechanics, still narrow content)

**Goal:** every core loop and subsystem working, proven on the atom + a small neighbourhood of topics. Mechanics breadth, not content breadth.

## Build
1. **Orchestrator** with the three reactive rules made real (switch-representation-on-failure, select-forward, detect-frustration), over a
   small multi-node graph; stable interface so a learned policy (Track 2) can swap in later.
2. **Create-anything** (`03-FLOWS/06`): Plexus compile + ontology mapping + verifier gate, on a constrained domain; placement + hand-off to Learn.
3. **Progress + knowledge twin** (`03-FLOWS/07`): the constellation map, twin queries (honest ranges), next-best, goal-gradient, cliffhangers.
4. **Meter + conversion** (`03-FLOWS/08`): behavioural budget, peak-cut **(elevated only)** with the "never before a real win" guarantee,
   conversion moments (week-3+ gating, aspiration-led), founding-member + referral queue. **No payment yet.**
5. **Parent companion** (`03-FLOWS/09`): WhatsApp digest generation (true-to-evidence), ask-anything, pride-then-opportunity. **Consent
   elevation seam present but real verification is Phase 4.**
6. **Safety + integrity** (`03-FLOWS/10`): moderation on all free-text, crisis escalation, integrity signals — first-class, fail-closed.
7. **Consent-tier enforcement** proven end to end: un-elevated path demonstrably runs with **zero** profiling capabilities wired.
8. **Today home** (`04-PAGES/01`) tying the ritual together.

## Gate (stop here)
Phase report + Phase 2 checklist. Demonstrate the un-elevated vs elevated difference explicitly. Approve before breadth.
